# Normalize all text to LF in the repo so Linux-deployed shell scripts and
# config never carry CRLF (a stray \r breaks the install.sh shebang on Ubuntu).
* text=auto eol=lf

# Shell scripts must always be LF.
*.sh text eol=lf

# Binary assets — never normalize.
*.pdf binary
*.png binary
*.jpg binary
*.ico binary
