# Feature Parity Tracker

Maps every functional area of the legacy Mar-Kov CMS to its build status in the new system. This is a **living document** — updated every increment. Source of the feature list: brief §1 + the User Guide's 24 chapters ([toc](docs/discovery/user-guide-toc.txt)) + verified against the live database.

**Legend:** ⬜ Not started · 🟡 In progress · ✅ Done (with tests) · ⏸️ Intentionally deferred (present in product but **0 rows** in this install — schema parity kept, UI deferred until needed)

> Status today (2026-07-09): **near parity.** The evidence sweep ([docs/PARITY_SWEEP_2026-07-09.md](docs/PARITY_SWEEP_2026-07-09.md)) dispositioned all 66 then-open rows with live legacy-DB + repo evidence: 48 closed (31 ✅ / 17 ⏸️), 18 remain open — every one a queued build (HANDOFF.md priority queue), led by QA sampling (native sample-set/Release creation), SH staging/assemblies, warehouse transfers + returns/credits, and MFA/SSO.

---

## 0. Platform foundations (brief §4.1)
| Feature | Status | Notes |
|---|---|---|
| Phase 0 schema & data discovery | ✅ | [SCHEMA_REPORT.md](docs/SCHEMA_REPORT.md) |
| Architecture proposal | ✅ | [ARCHITECTURE.md](docs/ARCHITECTURE.md) — approved |
| Monorepo + Docker + unattended Ubuntu installer | ✅ | Validated: build + up + smoke all green. **Versioned Prisma migrations now the deploy mechanism**: baseline `000000000000_init` generated from the schema (validated: fresh deploy ⇒ zero drift vs schema; existing db-push'd databases auto-baselined via the P3005 → `migrate resolve --applied` path in scripts/migrate.sh); CI applies migrations + **fails on schema/migration drift** (`migrate diff --exit-code`), so a schema change without a migration can't land green |
| CI (GitHub Actions) | ✅ | **Green on main** (run #116 on head `60ca3f9`, 2026-07-09; 116 runs to date). Three jobs: build + typecheck + unit tests; integration (Postgres 16 service, versioned `migrate deploy`, **schema/migration drift gate** `migrate diff --exit-code`, full API integration suite); Docker (compose config validation + image build). Runs on every push/PR to main |
| Auth — Argon2id, Redis sessions, lockout, session-fixation hardening | ✅ | login/logout/me/change-password live |
| Auth — MFA (TOTP), OIDC SSO | ⬜ | Modeled (`User.mfaSecret`/`ssoSubject`); not wired yet — no TOTP/OIDC library anywhere in apps/ (verified 2026-07-09). **Build queued**: legacy-usage deferral does NOT apply — there is no MFA/SSO in the legacy model; this is a committed greenfield security requirement (ARCHITECTURE.md: Argon2id + TOTP MFA + OIDC SSO/Entra). (1) TOTP enrollment (QR provisioning URI) + second login step + recovery codes/admin reset + optional e-sig factor; (2) OIDC authorization-code flow linking strictly by pre-provisioned `ssoSubject` (no JIT creation), issuer/client via the §14 settings registry |
| Users / Roles / Programs / Secured Items / Response Levels | ✅ | Schema + server enforcement (ProgramGuard) + Users admin UI (incl. **changing a user's group membership** — `PATCH /users/:id/roles`, with a last-active-administrator lockout guard) + **Roles admin UI now live**. **Roles admin** (`RolesService` / `GET\|POST\|PATCH\|DELETE /roles`, program `admin.roles`): create user-groups, grant/revoke their Programs (the screen-access matrix), edit name/description, delete (non-system, no users); system roles (ADMIN) are protected (no rename/re-scope/delete — their full grant is seeded). A **Roles** page (group list + create + per-group program-grant matrix grouped by folder). This is what makes RBAC + the approval engine usable for non-ADMIN groups (previously only ADMIN could be granted anything). **Secured-item admin UI now live too** (`SecuredItemsService` / `GET /secured-items`, `GET\|PATCH /secured-items/:id`, `PATCH /secured-items/:id/grants`, program `admin.securedItems`): edit a secured item's response level (require reason / signature / witness / disabled — operator-tunable; a disabled item fails safe and still forces a signature) and set which groups may **perform** (allow) or **witness** (allowWitness) it, via a **Secured Items** page. (Keys are seed-defined — the surface edits + grants, not create/delete.) **Enforcement now HTTP-tested**: an HTTP-layer suite (real Nest app + supertest, `apps/api/test/integration/http-layer.http.spec.ts`) asserts two route-table invariants over the whole registered route table — (a) *every* non-public route rejects anonymous access (401), and (b) *every* program-guarded route rejects a logged-in **zero-program** user (403) — so a dropped `@UseGuards` (anonymously reachable) **and** a dropped `@RequireProgram` (reachable without authorization but still 401 anonymously) are both caught. Plus depth on purchase-orders: 403 wrong-program / 200 granted, a method-level `@RequireProgram` overriding the controller default, and the global ValidationPipe (missing/typed/extra/nested fields, ParseIntPipe). **Remaining sub-parts closed by evidence (2026-07-09 sweep)**: (a) security-group row-segregation ⏸️ — legacy defines 6 `SecurityGroup` codes but zero data rows were ever assigned one (Item 21,176/21,176 = 'None', Ordr 75,948/75,948 = 'None', Recipe 19,104/19,104 = 'None', StorageRule 9/9 = 'None', Notification 14/14 = '*' wildcard) — the mechanism was never used in this install; (b) secured-item coverage audit **done** — of legacy's 80 `SecuredItem`s only 12 ever fired in 285,985 `LogSecuredItem` responses (Dec 2018→Jul 2026), only 3 remain enabled, and the only two with signature volume (OrderEditQtyExceeded 12,909, YieldOutsideTolerance 4,602) are covered by ERP1's stricter always-on `order.revise` / `order.complete` gates; `SecuredItemCondition` = 0 rows (conditional secured items never configured) |
| Approvals & Workflow chains | ✅ | All 8 legacy approval/workflow tables (`Workflow`/`WorkflowDetail`/`Approval`/`ApprovalDetail`/`RoleApprovalDetail`/…) are **0 rows** in this install, so the engine is being built **config-first, validated by tests/usage** rather than against legacy data (per the user). **Per-user-group approval policy now live**: a `RoleApprovalPolicy` (1:1 with `Role`) gives each group six independently-tweakable capabilities — Request approval / Approve / Approve update / Approve change / Override / No-approval-required (the legacy `Approval`/`ApprovalDetail`/`RoleApprovalDetail` model collapsed onto the role). `ApprovalPolicyService` + `GET /approval-policies` (every group with its effective policy — stored row or the safe request-only default) + `PATCH /approval-policies/:roleId` (partial merge, no-op short-circuit, atomic hash-chained audit), program `admin.approvalPolicies`. Starter groups (QA Manager / Supervisor / Operator) + defaults seeded (create-only, operator-tweakable). Web **Approvals** admin page (groups × capability checkboxes). **First ENFORCEMENT now live — QA lot disposition (backend):** `disposition()` is capability-aware — a group authorized to approve the change (Approve / Approve change / Override, or exempt via No-approval-required) enacts immediately (unchanged from before, incl. ADMIN), while a group that may only **Request approval** submits a **PENDING** `disposition_approval` (the Release is left unchanged) for a qualified approver to **approve** (enacting it) or **reject** via `POST /releases/approvals/:id/approve\|reject` + a `GET /releases/approvals` queue. Single-step; separation of duties (can't approve your own request); the PENDING→APPROVED/REJECTED transition is an atomic compare-and-swap (concurrency-safe); requester signs the request, approver signs the approval (e-sig ledger); fully audited. **Web now live too**: a **QA Approvals** queue page (approve/reject each pending request, with the approver's signature when required) + the Lot Trace disposition form shows a "submitted for approval" state when the actor's group may only request. **Reusable approval engine + order-edit blocking workflow now live**: a generic `ApprovalRequest` table + `ApprovalRequestService` (create / list-pending / atomic compare-and-swap decide) is the reusable blocking-approval engine. **Order edit** (`orders.edit`) now uses it — a request-only group's edit is held **PENDING** (the order is left unchanged) until a qualified approver (`canApproveUpdate` / approve / override) **approves** it (enacting the edit, re-validating NST) or **rejects** it, via `POST /orders/edit-approvals/:requestId/approve\|reject` + a `GET /orders/edit-approvals` queue; capable groups still enact directly. Separation of duties (can't approve your own request) + CAS (no double-enact). **PO + SH line edits now use the SAME blocking workflow** (`po.line.edit` / `sh.line.edit` kinds): each line action (add / update / remove) is submit-or-enact — a capable group enacts directly while a request-only group's change is held PENDING (the order unchanged) until a qualified approver enacts the exact requested op (re-validating Not-started under a row lock) or rejects it, via `GET\|POST /{purchase-orders,shipping-orders}/line-approvals[/:requestId/approve\|reject]`. **QA disposition has been migrated onto the shared engine too** (kind `release.disposition`) — one implementation of the blocking-approval lifecycle now backs all four consumers (order edit, PO lines, SH lines, disposition), with the e-signature and full audit preserved (the legacy `disposition_approval` table is retained-but-unused, to be dropped in a future coordinated migration). **Web approvals queues now live for all of it**: an **Order Edit Approvals** page, a combined **Line Edit Approvals** page (PO + SH sections), and the existing **QA Approvals** page; the order-edit and PO/SH line editors show a "submitted for approval" banner on a pending response. So the reusable blocking-approval engine is **complete and uniformly adopted**. **Multi-step ordered approval/workflow CHAINS ⏸️ with evidence**: all 10 legacy chain tables are 0 rows in this install (`Workflow`/`WorkflowDetail`/`Approval`/`ApprovalDetail`/`RoleApprovalDetail`/`SecuredItemCondition`/`SignatureMeaning`/`OrdrApprovedAddition`/`OrdrApprovedAdditionEdit`/`RecipeApprovedAddition`), and the only 'Approval Update' entries in 12+ years of Log are 6 rows from a single 2014-02-05 vendor session (`mar-kov\lex`) that persisted nothing — the plant never configured or used chains. Single-step blocking approval (the engine above) is the entire observed need |
| Supervisor override / approve-on-behalf | 🟡 | **Approve-on-behalf half done**: Override (`canOverride`) is enforced at every capability check — it qualifies a group to enact gated actions directly and to approve any pending request across all four blocking-approval consumers. Legacy-style at-station credential-swap was never used at the plant (all 17,512 signed secured-item responses in 285,985 were self-signed; zero signed-by-another, zero witness co-signs). **Remaining build (brief §5, queued)**: in-place elevation — when a secured action is blocked (missing perform-grant or request-only policy), a privileged user authenticates in the signature dialog, the server verifies their grant/Override, records BOTH identities on the ledger, and executes the original action in the same tx (blocked user stays logged in) |
| Audit trail (field-level, append-only, hash-chained) | ✅ | Live + `verifyChain` confirmed; advisory-lock serialized; atomic with mutations. **Web Audit viewer** added: searchable trail with expandable field-level diffs + one-click chain-integrity verification (now populated by the order-lifecycle actions) |
| Electronic-signature ledger | ✅ | **Capture flow live**: append-only hash-chained `ESignature` ledger (`ESignatureService`, own advisory-lock + `verifyChain`), wired into order **Complete** — the signer re-enters their password (Argon2 re-auth, lockout-tracked) and an optional second-person **witness** co-signs; the signature commits atomically with the status change + audit row. Driven by the `order.complete` **secured item** (`requireReason`/`requireSignature`/`requireWitness`, operator-configurable, seeded). Ledger viewer + integrity check at `GET /audit/signatures[/verify]`. **Signing now covers every signature-relevant action**: all five secured-item gates capture (order complete / reverse / revision publish, QA disposition request+approve, recipe publish) — which subsumes all legacy signature use: in 285,985 legacy secured-item responses (Dec 2018→Jul 2026) only OrderEditQtyExceeded (12,909) and YieldOutsideTolerance (4,602) ever captured signatures, both covered by the stricter always-on `order.revise`/`order.complete` gates. Witness co-sign supported though the plant never used it (0 of 285,985 responses). TOTP as an additional signing factor ships with the Auth MFA build (row above) |
| Reusable filterable/exportable grid (set-viewer platform) | ✅ | DataGrid: search/sort/paginate/CSV export; powers all module lists |
| Import/sync engine + reconciliation report | ✅ | **Log-driven incremental sync + reconciliation now live** (`POST /import/sync`, `GET /import/reconcile`, program `admin.import`). Discovery corrected Schema Report §9: legacy per-row `Version` is an optimistic-concurrency counter (NOT a watermark); the watermark is the **`Log` id** (666K rows, one per user operation) and **`LogResult`** (24.2M rows, one per affected ROW: TableName + key column + key value; indexed) is the authoritative delta feed — deletes detectable by re-pull-absence (no tombstones; verified on a real deleted OrdDetail). Full import now records `MAX(Log)` (captured BEFORE copying — overlap re-processed, never gapped) as `app_settings import.logWatermark`; **Sync changes** walks `LogResult WHERE Log > W`, dedupes touched keys, re-pulls per key column (handles `ItemCode`-keyed bulk ops, `Item`-keyed OrdDetail touches, comma-joined composite keys via the `AddressRef` view alias; parameterized, chunked, column names validated against INFORMATION_SCHEMA), upserts through the existing TableSpec maps, and propagates deletes conservatively (only PK-keyed id tables, never ids ≥ 1e9 — native rows are untouchable; natural-key tables never delete via sync), children before parents; genealogy re-derived only when its inputs moved; watermark advances only on success (idempotent re-run). **Reconciliation report**: per-table legacy `COUNT_BIG` vs mirror with native (ERP1) rows broken out — delta = (mirror − native) − legacy, `comparable=false` for natural-key tables — plus watermark lag (pending legacy operations). Web: **Sync changes** button + per-sync report (touched keys/upserted/deleted + unmirrored skips), **Compare with legacy** reconciliation table, run history with mode chips. Engine refactored onto a `LegacyDbService` seam (typed legacy access) so the whole flow is integration-tested against a fake legacy DB (10 tests: watermark foundation, keyed re-pull, delete safety incl. native range, alias/composite keys, unresolvable-key full-recopy fallback, failed-sync watermark hold, reconcile math) |

## 1. Master data (UG ch.2)
| Feature | Status | Notes |
|---|---|---|
| Items (incl. names, packages, packaging prototypes, services) | 🟡 | Item core list/search/context filter (SUNDRY/PP/NAME/PROTOTYPE/PACKAGE)/create + PATCH update API live. **Deferred by evidence (2026-07-09)**: services (0 items with Service/ServiceGroup, no SERVICE context — never used); ItemPkg (62 rows ALL identical defaults — zero information); prototype bulk-propagation (legacy Packaging Prototype Update was a RecipeDetail rewrite tool, architecturally superseded by ERP1's read-time packout-binding resolution). **Remaining build (queued)**: (1) NAME alias management — all 7,375 NAME items are pure `ReplacedBy` aliases (0 OrdDetail/RecipeDetail/Lot refs) and Item Name Update is the plant's 2nd-busiest master-data program (8,787 ops, live); (2) web item-edit form incl. the ItemEntity ST planning knobs (min stock / lead times — planning reads them, nothing natively edits them); (3) native ItemPackagedProduct binding create (no native create exists, so a NEW packaged product can't be made orderable today) |
| Item chemical/safety, custom, components, units, tests, kits | ⏸️ | Item tests ✅ (QA item-tests CRUD editor, programs `qa.itemTests`/`qa.itemTestsEdit`; `ItemTest` mirrored). Rest ⏸️ by evidence (2026-07-09): ItemChemical (13,739 rows) is auto-created defaults — every populated safety column a single constant (DisposalGroup='PENDING' on ALL rows, HMIS=0, PPE='A'; DG/UN/kosher/halal/SDS/shipping-name all 0 rows), only row-creation writes in 15y of Log; ItemCustom likewise (only [Group] populated, 3 static values, one write/row); ItemComponent 0 rows; kits ItemKitID 0 rows + 0 IsKit items; ItemUnit 2,265 all-default rows (BaseQty=1 everywhere, 0 UPCs), dead since 2020-04-27 — ERP1 deliberately sources per-line packaging from OrdDetailPricing instead (ASSUMPTIONS §viewers) |
| Suppliers & Manufacturers | 🟡 | Role/terms management ✅: Entities module list/search/role filter/create/edit with all role flags + inactive/currency/terms/customerType, audited. **Remaining build (queued, shared with the Customers row)**: entity address-book CRUD — add/edit/remove `Address` rows + `AddressReference` links (Main/Remit/Ship) with the document-facing fields; legacy address maintenance is live across ALL party programs (708 BillTo + 472 ShipTo + 351 Supplier AddressRef touches, latest 2026-07-08) and the 20 Address columns feed the To/Ship-To blocks on documents ERP1 already renders. Entity memos ⏸️ (395 of 28,066 MemoRef rows — the rest ride Trans/Ordr) |
| Customers & Ship-Tos | 🟡 | List/create/edit/role filters, customer terms/currency/type, price-list assignment (PriceLists editor) ✅. **Remaining build (queued)**: the shared entity address-book CRUD (row above) + expose `Entity.Parent` on ship-to create/edit (ship-to hierarchy under a customer; field already mirrored). EntityDocument ⏸️ with evidence: 4,284 rows = exactly 6 document-type rows × 714 entities, EVERY row identical default flags (Order/BillTo/Print all False, Format null) — auto-created boilerplate, never configured in 15y |
| Salesmen, Ship Via | ✅ | Managed as Entity role flags (IsSalesman/IsShipVia) in the Entities admin — create + edit + role filters; orders reference both via pickers (Ordr.Salesman set on 98% of shipping orders — already wired); audited. Party addresses ride the shared entity address editor (rows above). Legacy usage thin: Ship Via Update 59 ops ever, Sales Rep Update 14, Salesman Update 8 (last 2019). EntityFreightService ⏸️ (23 rows, last write 2023-08-21) |
| Warehouses & Labs | ✅ | Both managed as Entity role flags in the Entities admin (create + edit + role filter); isLab wired end-to-end even though this install has 0 lab entities. Legacy Warehouse/Lab Update: 19 uses in 15y, dormant since 2020-10-22 — nothing further to build; warehouse-consuming behavior (receiving/production locations, transfers) lives in the inventory/config modules |
| Pricing (price versions & price lists) | 🟡 | **`PriceVersion` (615) + `PriceDetail` (15,745) mirrored + imported** — a supplier's effective-dated pricing: per-item packaging (`PkgType`/`EntityQuantity`/`EntityUnit`/`PriceByPackage`), the supplier's item code (`EntityItemCode`), quantity-break tiered prices (`MinOrder1/Price1`..5) and lead time. The effective version for a supplier = latest `EffectiveDate` ≤ today. This is where Mar-Kov configures purchasing packaging; it now flows onto PO lines (see §2). **Sales price lists now have a read+write master-data EDITOR** (`SalesPricingService` / `GET\|POST /price-lists`, programs `sales.priceLists` browse + `sales.priceListEditor` write): a price list is an `Entity` flagged `IsPriceList` that owns effective-dated `PriceVersion`s of per-item `PriceDetail`s (the same base tables, keyed off `InvItem` for sales — added `PriceDetail.invItemId`/`verifiedDate`); customers reference it via `Entity.PriceList`. Create lists (native `Entity`+`Address`+`AddressReference`, ids ≥1e9), versions, and details; assign/unassign customers; `priceForCustomer` resolves a customer→list→effective-version→tiered price (mirrors purchasing line-sourcing; consumed by SH order pricing — see below). This install has **no sales price data** (0 `IsPriceList` entities) — built for near-term use per the user (validated by integration tests, not live data). **Web editor UI ✅** (PriceLists.tsx, route /price-lists over SalesPricingService) and **sales-order price sourcing ✅** (shipping.service.ts priceForCustomer at SH create, add-line, and a price-quote endpoint) — both former pending items were stale (2026-07-09 sweep). **Remaining build (queued): the SUPPLIER price-version editor** — legacy 'PriceVersion Update' is a top-10 program (1,998 ops, live daily, latest 2026-07-08) maintaining the 615 PriceVersion / 15,745 PriceDetail supplier rows that ERP1's PO line-sourcing reads; ERP1's only PriceVersion/PriceDetail writes are hard-gated to IsPriceList entities (assertVersionOnList), so supplier pricing is unmaintainable after cutover |
| Units, zones, bins, location groups/categories | ✅ | Units ✅ (master units editor: list/get/create/update + audit, /units page; legacy Unit = 16 rows, Unit Update ran 3× in 15y, last 2018-12-13). Zones ⏸️ — 3 static ZON Location rows (PREWEIGH/BATCHING/PACKAGING), exactly one distinct value per Ordr zone column across 75K orders, no zone-editor program in Log: fixed plumbing, not managed data. Bins ⏸️ (Bin Update 7 uses ever, last 2020-09-25, wrote 3 Location touches; no Bin table). Location groups ⏸️ (LocationGroup 0 rows) |

## 2. Purchasing & receiving (UG ch.3)
| Feature | Status | Notes |
|---|---|---|
| Purchase Orders | ✅ | `Ordr` Context=`PO` (4,090 imported; also browse via the unified Orders type filter). **Native PO creation now live**: `POST /purchase-orders` (program `purchasing.create`) creates an `Ordr` Context=`PO` for a supplier (Entity = supplier) with one or more `OrdDetail` Context=`PO` lines (item, qty, optional unit price/unit), born Not-started, native ids ≥1e9 under the shared id-allocation advisory lock, atomic hash-chained audit. A dedicated **Purchasing** page (supplier + item typeaheads, line editor) + a **print-faithful Purchase Order document** (`GET /purchase-orders/:id`, program `purchasing.po`) **reconstructed field-for-field against the plant's real PO** (validated on PO 189229): To (supplier) / Ship To (our org Owner, resolved data-drivenly) blocks, Terms / FOB (`IncoTerms` lookup) / Carrier, line table with the supplier packaging detail (`SupQty`+`OrdDetailPricing` → "1 DRUM / 400 lb per DRUM"), the supplier's "Your Code" (`OrdDetailPricing.EntityItemCode`), Price-per-unit, Value, Total, and the standard **Terms & Conditions** page 2. A driver **PO Pickup** copy (`/purchase-orders/:id/pickup`) renders the same doc without any pricing. Native POs render in the same template. **Native PO lines now auto-source supplier packaging + price from the supplier's effective price version** (`PriceVersion`/`PriceDetail`, the Mar-Kov way — not hand-typed): creating a PO line snapshots the matching price detail's package type, qty/package, unit, "Your Code" and price-by-package flag into `OrdDetailPricing`, and defaults the line price to the quantity-break tier price — so a natively-created PO renders the same "2 BAG / 44 lb per BAG / Your Code" detail imported POs do (validated end-to-end on supplier 100866). Complements the supplier AP bills. **Line-level edits now live (backend)**: add / update / remove a line on a not-started PO (`POST` / `PATCH` / `DELETE /purchase-orders/:id/lines[/:lineId]`, gated `purchasing.create`) — adding a line re-sources the supplier's packaging + tier price from the effective price version (exactly like create); IDOR-safe, NST-only, refuses removing the last line or a line that already has receipts (and won't drop a line's ordered qty below the received quantity). (The legacy `OrdrEdit`/`OrdDetailEdit` edit-revision staging tables are 0-row/unused in this install — deferred; edits apply directly to the live order.) **Web line-editor live**: an "Edit lines" panel on not-started POs (edit qty/unit/price in place, remove, or add an item — supplier price + packaging sourced automatically). The same line-edit capability now exists for SH orders (see §9). **Approval routing shipped**: PO line edits (add/update/remove) run on the shared blocking-approval engine (kind `po.line.edit`) — a request-only group's change is held PENDING until a qualified approver enacts or rejects it via `GET\|POST /purchase-orders/line-approvals[/:requestId/approve\|reject]` (see §0); multi-step ordered approval chains stay deferred on the §0 Approvals row (legacy workflow tables 0-row) |
| Purchase Receipts | ✅ | `ChangeSetReceipt` (9,427 rows) **mirrored + imported** (1:1 with its receipt `ChangeSet`; PK = ChangeSet — one ChangeSet per received line). **Native receiving + lot assignment now live**: `POST /purchase-orders/:id/receive` (program `purchasing.receive`) records a receipt with one or more **lots per line** (split a delivery across the manufacturer lots received). For each lot it assigns a **system lot number** (raw-material sequence from 100), creates the `Lot` (supplier-tagged, `ManfLot`/`SupLot` = the **required** manufacturer lot — the recall key) + its `Sublot` + a Context='PO' `ChangeSet` (native ids ≥1e9 under the shared lock) + a `ChangeSetReceipt` (sublot-linked, PSQty), and bumps `OrdDetail.QtyUsed` (atomic COALESCE). Over-receipt allowed, closed POs rejected, IDOR-safe, atomic hash-chained audit. The Purchasing panel shows ordered/received/backordered per line + receipt history (our lot + mfr lot) + a multi-lot **Receive** form; a **Recall lookup** (`GET /purchase-orders/recall`) finds received lots by manufacturer lot number (item, supplier, qty, PO). **On-hand is minted at receiving** by the valuation engine (configurable receiving-location setting) with per-lot unit cost from the PO line price + InvMovement legs; receipt cancellation (UG 3.4.2) = the §3 receipt reversal (`POST /inventory/receipts/:id/reverse`). Return-to-supplier (⏸️) and misc receipts (✅) are tracked on their own rows |
| Return to Supplier | ⏸️ | `ChangeSetReturn` — only 18 rows in this install (effectively unused); deferred per the 0-row/unused-module rule. Schema parity later if needed |
| Miscellaneous / Create Inventory receipts | ✅ | **Native misc (non-PO) receipts live**: `POST /inventory-receipts` (program `inventory.receipts`) creates stock without a purchase order (opening balances, found stock, samples in) — legacy `ChangeSet` Context=`MISC` (Ordr null) + a 1:1 `ChangeSetReceipt` (OrdDetail null, Item + PSQty), verified against live data (424 MISC receipts). Each line mints a system lot (the shared raw-material sequence) + sublot + on-hand at the receiving location via the valuation engine — mirrors purchase receiving, minus the supplier/PO line, so the manufacturer lot is optional (set it to make the lot recall-findable). `GET /inventory-receipts` browses them. Web **Inventory Receipts** page (multi-line create form + recent list). Reverse shipped — `POST /inventory/receipts/:id/reverse` (program `inventory.reverse`) with a Reverse action on the page (see §3 reversal row); adjustment-out is the audited inventory adjust (write-on/write-off with required reason, COUNT ChangeSet, movement leg) |
| Create Sublot | ⏸️ | Never used: every one of the 55,854 lots has exactly ONE sublot (0 multi-sublot lots in 15 years; the only %Sublot% program in Log is Disposition Sublot 25,458 = QA disposition, live natively). ERP1 mints the 1:1 sublot automatically at receiving / misc receipt / lot-enablement / batch completion — a manual extra-sublot screen would be dead UI |
| Purchase price detail sets | 🟡 | **Purchase Price Detail Set Viewer** (`GET /purchase-orders/price-details?supplierId=`, program `purchasing.priceDetails`) lists a supplier's current price details (item, package type, qty/package, their-code, price, lead time) from its effective price version. Also drives native PO creation: `GET /purchase-orders/price-detail?supplierId&itemId&qty` (program `purchasing.create`) resolves the supplier's packaging + tiered price for a line. **Remaining build (queued): the supplier price-detail-set EDITOR** — create an effective-dated PriceVersion for a supplier + CRUD its PriceDetails (item, package type/qty-per-package/unit, supplier item code, quantity-break tiers MinOrder1–5/Price1–5, lead time, price-by-package); legacy 'PriceVersion Update' = 1,998 uses, roughly weekly and current (2026 YTD: 134), yet ERP1 has zero supplier-side write endpoints today; mirrors the SalesPricingService price-list editor on the same base tables |

## 3. Inventory management (UG ch.4)
| Feature | Status | Notes |
|---|---|---|
| Inventory status & sublot expiry | ✅ | Inventory browser + import live (37,934 parcels; status filter, adjust/move/reverse actions). Sublot expiry closed by evidence: the plant never set one — Release.ExpiryDate is NULL on all 80,400 releases (0 future-dated) and Sublot/Lot have no expiry column in this install. ERP1 models expiry natively anyway (QA disposition sets it, planning flags Expired/Retest), so it works if ever adopted; the automatic-expiry job stays tracked on the §11 QA row |
| Costing (standard, replacement, actual) | ✅ | Actual costing = per-lot unitCost + valuation engine (specific identification for lot-traced, FIFO oldest-first for untracked; consumed extended cost Σ(qty × that lot's own unitCost) rolls into the produced lot), validated exact vs the encrypted vendor cost functions (order cost 12/12, at-date exact — §18). Standard cost ⏸️ by evidence: 0/21,176 items carry a StandardCost, 0/273,732 OrdrItemCost rows have UnitStandardCost, StandardValue=0 on every movement. Replacement cost ⏸️ superseded: legacy auto-maintained it as a latest-purchase-price cache (verified Item.ReplacementCost == latest PO line price on current orders); ERP1 reads current cost live from the supplier effective price version (PO sourcing, recipe pricing rollup) |
| Lot-tracking enablement (per item) | ✅ | **New `inventory.lotTracking` module**: `Item.lotTracked` flag (items default FIFO-by-qty, not traced). `POST /lot-tracking/items/:id/enable` captures opening on-hand **by lot, grouped per location** — raw materials enter a vendor/mfr lot and ERP1 **mints a lot number** (sequential from 100, tagged supplier+vendor lot, for relabeling); finished goods enter the existing lot number — creating Lot+Sublot+Inventory and **wiping the item's prior legacy on-hand**. `/disable` reverts to FIFO. Web **Lot Tracking** page (item list + enable form showing the assigned lot numbers). Foundation for recall (recall is inert until an item is enabled). FIFO consumption (valuation engine), batch-consume lineage (consume-lots / per-line execution recording) and shipment-at-close capture (ship-lots → shipment_lot) all shipped — recall is live end-to-end for enabled items |
| Storage rules | ⏸️ | Never enforced at this plant: 9 StorageRule status-pair rows exist but `ParamsPurchaseReceipt.ValidateStorageRules=False` live (re-verified 2026-07-09) and zero %Storage% program uses in 15 years of Log. Schema parity only if ever requested |
| Adjust / consume / reweigh / remeasure | ✅ | **Consume now live** as part of the valuation engine: batch orders deplete on-hand by specific lot (`consume-lots`, lot-traced) or FIFO by quantity (`consume-qty`, not-traced); shipping orders deplete shipped finished-good lots at close (`ship-lots`); on-hand is **minted** at purchase receiving (configurable receiving location) and at batch completion (produced lot, configurable production location). **Adjust now live**: `POST /inventory/adjust` (program `inventory.adjust`) sets an on-hand parcel to a counted quantity (write-on / write-off) with a required reason — records a legacy `ChangeSet` Context=`COUNT` header (native id) + the `Inventory.qty` change, atomic + audited (before→after; the current qty is re-read under the id-allocation lock so the recorded delta reflects any concurrent adjust). An **Adjust** action on each Inventory row opens a counted-qty + reason modal. Reweigh/remeasure closed by evidence — ZERO uses in 15 years of Log (no Program or Action ever matched %Reweigh%/%Remeasure%); the audited adjust covers set-to-weighed-qty and fires the legacy Reweigh Outside Threshold notification past the configurable threshold (`inventory.reweighThreshold`, legacy live value 5%) |
| Empty / merge / dispense from containers | ⏸️ | Never used: 15 years of Log contain zero Empty Container / Merge Containers / Dispense From Container operations (the only %Container% programs are label printing — PrintContainerLabel 25,434, ContainerLabelReprint 21). Containers here are VSL Location rows ≈1:1 with sublots (label targets only); ERP1's sublot-level (item, sublot, location) parcel model covers every stock operation the plant performs |
| Transfer cans & tanks (create/refill) | ⏸️ | Never used: 0 Location rows with TransferCan=1 (no transfer can or tank ever created) and zero %Transfer%/%Tank% program uses in 15 years of Log. Plain location transfers (172 legacy TRNSFR) are covered by the live inventory transfer |
| Container & lot disposal + reversal | 🟡 | **Receipt reversal now live**: `POST /inventory/receipts/:changeSetId/reverse` (program `inventory.reverse`) reverses a posted purchase (PO) or miscellaneous (MISC) receipt — but only while its minted stock is **still untouched** (exactly the one parcel it created, holding the full received qty; anything consumed / moved / split / adjusted is refused). Writes a reversing `ChangeSet` (Context `RVS`+original, `reverseChangeSet` back-pointer), removes the minted on-hand, and for a PO unwinds the `OrdDetail.QtyUsed` bump — all under the id-allocation lock (the dup-check + untouched-check run **inside** the locked tx, so two concurrent reversals enact exactly once). A **Reverse** action on the Inventory Receipts page (MISC; PO-receipt reverse UI is a follow-up). Disposal of in-use stock is a write-off via the inventory **adjust** (to zero, with a reason). **Batch (order) reversal now live too — see §5.** Container/lot disposal ⏸️ by evidence: no disposal program in 15 years of Log (the only %Dispos% program is QA's Disposition Sublot) and no disposal context exists in the full ChangeSet census — write-offs run through the audited adjust. **Remaining build (queued): shipment reversal** — genuinely used and ongoing (RVSSH = 293 reversing change sets, every year 2019–2026, 29 in 2026 YTD): restore shipped on-hand from shipment_lot, unwind OrdDetail.QtyUsed, reversing RVSSH ChangeSet + reversed SH movement legs, refuse when already invoiced, e-signed like the other reversals. Plus trivial follow-up: surface the existing PO-receipt reverse endpoint in the Purchasing UI |
| Move & staging | ✅ | **Location-to-location transfer now live**: `POST /inventory/transfer` (program `inventory.transfer`) moves a quantity of an on-hand parcel to another location — deducts the source, merges into an existing same-item + same-lot + same-status parcel at the destination (else mints a new parcel there, native id), records a legacy `ChangeSet` Context=`TRNSFR` header, atomic + audited; total on-hand is conserved, over-moves / same-location / non-positive rejected, source qty re-read under the id-alloc lock. A **Move** action on each Inventory row opens a destination-location picker (`GET /inventory/location-options`) + qty. Staging closed by evidence — zero %Staging% program uses in 15 years and LcnMovement (99,018 rows) holds only CREATE 98,846 + TRNSFR 172 (no staging context ever); LcnMovement is the vendor's internal container-creation/transfer log, covered by ERP1 ChangeSets + hash-chained audit |
| Inventory count, cycle count, verify location, mark/write-off missing | ⬜ | **Build queued: the Inventory Count workflow** — heavily used and current (Log 'Inventory Count' 3,129 uses, steady, 226 in 2026 YTD; InventoryCount 1,497 headers + InventoryCountDetail 21,047 lines, 1:1 with the COUNT ChangeSets ERP1's adjust already writes natively): mirror + import both tables; native count sheets (create a count scoped by location/item/status → capture counted qtys per parcel, book vs counted, QtyAdjust computed → post as per-parcel adjustments through the existing adjust/valuation machinery under ONE COUNT ChangeSet; unposted counts editable); an Inventory Counts page. Cycle count ⏸️ (0 Log uses ever); verify location ⏸️ (0 uses); mark missing ⏸️ (1 use in 15y); write-off-missing = the audited adjust |
| Trace children / trace parents (genealogy) | ✅ | Lot-level lineage **now live**. `SublotParent` and every sublot/cost linkage column are empty in this install (verified by a multi-angle sweep), so lineage is **derived** into `lot_genealogy` from `OrdDetailCommit` (consumed-lot→produced-lot via `Lot.OrdDetail`; 27.5K edges, cross-validated 100% against the packaging-movement path). Recall/trace traverse it with recursive CTEs + upstream provenance (producing order + `LotIngredient` item composition). **Raw→batch lineage now captured going forward**: `POST /orders/:id/consume-lots` (program `orders.consume`) records the raw-material lots a batch consumed as `lot_genealogy` edges (source='consumption', preserved across re-derive), so a recall traces a raw lot forward to the batches (and their packouts) it went into; a "Record consumed raw lots" control on the MFBA order. (Capture only — on-hand depletion + cost roll-up is the valuation engine.) **Forward shipment lineage now captured too**: closing a shipping (SH) order records the finished-good lot(s) shipped (`shipment_lot`), so a recall now lists the **shipments** each affected lot reached — customer / PO# / ship date / qty — alongside on-hand. **Recall is now first-class for both entry modes**: `GET /recall?q=` resolves the term to a finished-good/batch/packout lot (direct) **or** a raw-material lot by the supplier's **manufacturer lot** (scoped to `SupLot IS NOT NULL`, then forward-traces raw → batch → packout → shipment), surfacing how it matched + the focus lot's kind (raw vs batch/packout/FG). The Recall page is the unified entry (deep-linkable `/recall?q=<lot>`); the purchasing manufacturer-lot recall **links each received raw lot into the full forward recall** ("trace forward →"). Honest limits surfaced in UI: legacy history has only the one packaging hop, sublot==lot here, no supplier-lot trace |
| Label printing / reprinting | ⬜ | **Build queued** — heavily used, ongoing daily: PrintContainerLabel 25,434 uses (2,651 in 2026 YTD; also clears the label-required flag — LabelRequiredContainer currently 30); PrintShippingLabel 17,666 (1,668 YTD), 1:1 with the 17,665 ASM pallet locations. Two print-faithful label documents on the existing HTML print-doc pattern: (a) container/lot label (item + description, our lot, manufacturer lot, qty/unit, received/made date, status) printable from receipt lines, inventory parcels, produced lots, with reprint; (b) shipping label (ship-to address, customer + customer PO, carrier) from an SH order. No format designer — LabelFormat 0 rows (formats were client-side) |
| Update container/lot/sublot info | ⏸️ | Never used: zero Update Container/Lot/Sublot uses in 15 years of Log — the only lot-editing the plant ever does is Disposition Sublot (25,458 uses), live natively as the e-signed QA disposition (status/grade/purity/expiry). Field corrections (e.g. a mistyped manufacturer lot) would be a new ask, not parity |
| Costing (standard, replacement, actual) | ⏸️ | Duplicate of the costing row above (see it for the full disposition). `InventoryCost*` is the vendor's INTERNAL actual-cost ledger (InventoryCost 71,856 / InventoryUnitCost 68,960 rows, still written by the running legacy system; InventoryCosts/InventoryCostAtDate are views — the current-cost view/fn GetInventoryCosts returns ZERO rows install-wide) — replaced by ERP1's per-lot unitCost + valuation engine, validated exact against the encrypted cost functions; standard cost is 0 on every row in this install |

## 4. Manufacturing recipes (UG ch.5)
| Feature | Status | Notes |
|---|---|---|
| Batching recipes (ingredients, components, coatings, packages, procedures, IPTs, planning) | ✅ | **Recipe lifecycle management now live** (`RecipeEditorService`, program `recipe.editor` write / `recipe.manager` browse). Deep discovery first (live DB + UG ch.5 + release notes): **versioning is a `.NN` suffix on RecipeNumber** (every number unique; `ParamsRecipeManager`: VersionSeparator '.', VersionLength 2, **SingleActiveRecipe=True**; the `Version` column is just a save counter); recipes are stored **normalized per 1 lb of product** (5,650/5,652 active RMBA: PK qty=1.0, UI Σ=1.0); the procedure IS the RecipeDetail tree (BA root → UI/INSTR in ExecOrder → PK child; XML null on all 19,074 rows); this install uses NONE of the vendor's advanced editor features (no Add Methods/purity/percent-modes/Use Groups/Adjust/Record Params/libraries — all 100% unused). Editor scope = observed usage (docs/ASSUMPTIONS.md): **create draft** (native ids ≥1e9, BA+PK structure minted per live convention incl. TotalWeight 0.4536 = 1 lb in kg), **edit draft header** (comment=revision note, reference, lead time, rework, re-point product) and **procedure** (`PUT /recipes/:id/procedure` — ordered ingredient/instruction lines authored at any formula basis, normalized ÷basis on save; in-place updates diffed, adds/removes audited per line; ExecOrder/Line renumbered like legacy), **clone → next `.NN`** (all lines copied verbatim incl. UB/useFrom, Parent tree remapped), **delete draft** (7.18 parity; published recipes immutable & undeletable). Schema gap closed: `RecipeDetail.UseFrom`/`TotalVolume`/`TotalVolumePercent` now mirrored + imported (UseFrom populated on all 11,893 UB lines; semantics opaque — native recipes omit UB, see OPEN_QUESTIONS). Full **Recipes web page rebuild**: list (type + Draft/Active/Inactive filters, revision-note column), detail (status badges, version-family chips, product), draft editor (item typeaheads, move/remove/add rows, live Σ-vs-basis check), publish dialog, pricing panel, preview link. Advanced vendor features ⏸️ with evidence (2026-07-09): RecipeDetail context census is ONLY UI/INSTR/PK/BA/UB/IPT/FMT/GRP (105,408/29,147/19,103/11,916/11,908/59/3/1) — no vessel/phase/coating/Use-Group/Adjust/planning-task line contexts exist anywhere in the install; RecipeDetailResource and Resource are 0 rows install-wide (no vessel was ever defined). Recipe-level IPT steps ⏸️ by observed usage: 59 lines ever, 58 on inactive recipes; order QC flows from ItemTest (13,524 rows) at order creation and POST /orders copies legacy recipe IPT lines as-is, so nothing is lost |
| Verify / publish / activate, batch-record preview | ✅ | **Publish** (`POST /recipes/:id/publish`) runs **Verify** first (vendor: publish auto-verifies — product line present, ≥1 ingredient, complete ingredient lines, revision comment required; standalone `GET /recipes/:id/verify` too) then enforces **single-active-recipe**: other active published recipes of the same product+context are deactivated in the SAME transaction (**rework recipes exempt both ways**, vendor 7.21). Gated by the new operator-configurable **`recipe.publish` secured item** (default reason-only; signature/witness enableable → Argon2 re-auth + optional witness → hash-chained `ESignature` linked to the audit row, same fail-safe pattern as order-complete/disposition). **Activate/Deactivate** (`POST /recipes/:id/active`): published-only Mode toggle; re-activating an old revision atomically deactivates the current one. **No unpublish** (vendor-confirmed; revise = clone). **Batch-record preview live**: `GET /recipes/:id/preview?batchSize=` renders the batch-sheet payload straight from RecipeDetail (scaled, grams-threshold, QC specs from the product's ItemTest) — same shape as the order batch sheet; web `/recipes/:id/preview` reuses the shared `BatchSheetView` with a rescale control + "no lot assigned" banner (vendor §5.1.14 caution). **Order-creation enforcement closed**: `POST /orders` now rejects drafts + inactive (superseded) revisions and the picker filters them — previously unenforced (moot for imported data, load-bearing the moment the editor publishes `.NN+1`). **Concurrency-hardened after multi-agent review**: every recipe mutation serializes on the shared advisory lock with authoritative state re-read INSIDE the transaction (no double publish, no two-active race, no edit/delete racing a publish); the `recipe.publish` PERFORM grant + contemporaneous reason are enforced; pricing gated by `purchasing.priceDetails`. Flow-tested end-to-end (structure/normalization/IDOR/immutability/single-active/rework/e-sig/grant+reason/double-publish/enforcement/preview/pricing) |
| Batching Recipe Library (groups, phases, instructions, formats, IPTs) | ⏸️ | Effectively unused in this install (1 empty 'DEFAULT' RMBAL row; library Format/steps never referenced; RecipeDetailParams 2 rows) — deferred per the unused-module rule |
| Packaging recipes & Packaging Recipe Library | ✅ | Same editor handles RMPP: flat **PK root + UI components** (verified: live RMPP recipes contain ONLY PK+UI lines), per-1-lb quantities, same publish/single-active/clone lifecycle (packaging has no rework exemption — none are rework). Packaging Recipe Library ⏸️ with evidence: Recipe context census (RMBA 11,916 / RMPP 7,151 / RMPR 36 / RMBAL 1) shows NO packaging-library (RMPPL) recipe ever existed in this install. **`ItemPackagedProduct` (bulk→packout binding, 7,136 rows) now mirrored + imported (full + log-driven sync)** and drives §5 specify-packouts + §6 packaging-product lookup; the bound recipe resolves to the ACTIVE revision at read time (ERP1 publish doesn't rewrite bindings — see ASSUMPTIONS §Packouts) |
| Recipe pricing & expected costs | 🟡 | **Expected-cost rollup live** (`GET /recipes/:id/pricing?batchSize=`): vendor §5.3.1 algorithm — per ingredient, every quantity-break tier of every supplier's **effective price version** evaluated (order max(needed, tierMin) at tier price), cheapest supplier wins; **Excess Qty/Cost** reported when a tier minimum overshoots; falls back to `Item.standardCost`; unpriced ingredients surfaced. Pure tier math unit-tested (`recipe-pricing.ts`, vendor's worked example). Web pricing panel (batch-size input, per-line supplier/unit/order-qty/cost/excess + totals). **Remaining build (queued): sub-recipe recursion** — genuinely load-bearing: 52% of UI lines in active published recipes reference a recipe-made ingredient (23,680/45,366; only 83 of those 5,702 sub-recipe items have any supplier price, and Item.StandardCost/PurchasePrice are 0/NULL on ALL 21,176 items, so the current fallback never fires with a real value — over half of a typical rollup prices at nothing today). Item.CostingRecipe is set on 12,823 items, 12,809 pointing at the currently ACTIVE revision → resolve-to-active-at-read-time + recurse the §5.3.1 rollup (memoized, cycle guard); Item.ReplacementCost (12,334 items) as terminal fallback; Item.CostingRecipe is already mirrored + imported (legacy-import Item mapping), so this is a pure rollup extension |
| Recipe mass-substitution (`RecipeReplacement*`) | ✅ | **Native rebuild live** (`RecipeReplacementService`, program `recipe.editor`): `GET /recipes/replacement/preview?fromItemId=` lists the ACTIVE PUBLISHED recipes using an ingredient (with product + qty); `POST /recipes/replacement` runs the job — per selected recipe **clone to the next `.NN` → swap the ingredient on the clone (same quantities, per-line audit) → optionally publish** (single-active rule deactivates the superseded revision; e-sig honored when required). Per-recipe failures are recorded per row and the job continues (legacy Status/Error semantics); an unpublished failure leaves a harmless deletable draft; a job-level audit row summarizes the run. Web **Replace ingredient** panel on the Recipes page (from/to pickers, affected-recipe checklist, publish toggle + password, per-row results). The legacy job tables themselves (54/59/3,418 rows) are historical work-queue records — not imported (the audit trail is the ERP1 record) |

## 5. Batch order processing (UG ch.6–7)
| Feature | Status | Notes |
|---|---|---|
| Create orders from recipes; import orders | ✅ | `Ordr`/`OrdDetail` mirrored + imported (75K/505K rows); unified **Orders browser** (type filter PO/MFBA/MFPP/SH, search, hold/open filters) with full line detail + party/item/recipe decoration. **Native batch-order creation now live**: pick an RMBA recipe + batch size on the Orders page → `POST /orders` (program `orders.create`) scales every `RecipeDetail` line into `OrdDetail` (UI ingredients + PK product × batch size; structural/instruction lines copied), seeds the product's `ItemTest` OnProduction specs onto an IPT line as `OrdDetailTest` (so the batch ticket's QC section is populated), born Not-started and flowing straight into batch-sheet → release → complete → close. Atomic hash-chained audit; native ids in a high range (≥1e9) so a later legacy import can't clobber them. **Finished-good lot now minted at creation** per the plant convention `YYMMDD###` (### = the next lot sequence for the day, shared across MFBA+MFPP, computed under the id-alloc lock), linked to the product (PK) line via `Lot.OrdDetail` (the lot of record) and stamped on `Ordr.ManfLot`. Vendor order-file import ⏸️ with evidence: the only import programs in 15 years of Log are QuickBooksImportVendors (454) / QuickBooksImportCustomers (102) — no order-import program ever ran. Multi-batch ⏸️ (§5 variance row: Ordr.Parent NULL on all 75K); execution live (§5 execution rows) |
| Release, specify packouts, print batch sheets | ✅ | **Batch ticket** reconstructed to match the plant's real paper format (validated field-for-field vs their PDF on order 189170): header (Formula#/recipe, Batch & Required dates, product + total weight, Batch Order, This Lot, Last Lot=prior lot of same item, Customer), Procedure (raw-material lines w/ Grams\|Pounds\|Done + inline instructions), blank Batch Additions, Quality Control (Test\|Specification from `OrdDetailTest` Min/Max\|Result), blank Packaging, and QC'd/Weighed/Mixed/Packed/Closed-by sign-offs. Server-side `GET /orders/:id/batch-sheet`. **Specify packouts (UG §6.4) now live**: a **Packouts panel** on every production order — on an MFBA batch it shows the demand table (packaging orders allocated to this batch via `OrdDetailCommit`, with status/lot/allocated bulk), Total-yield/allocated/remaining totals (negative remaining = the vendor's over-packout warning, never a block), and the product's packout options (`ItemPackagedProduct`) with can-make math; `POST /orders/:id/packouts` (program `orders.create`) creates the MFPP order from the packout's ACTIVE recipe revision and allocates this batch's bulk to it (native-id `OrdDetailCommit`: new order's bulk UI line ← this PK line — the exact linkage all 27,866 live commitments use) in ONE transaction (batch row-lock → id-alloc advisory lock; editable until completion, per vendor). Editable Supplied Qty (≤ requirement) supports multi-batch supply. On an MFPP order the same panel shows the supply side (which batch feeds it, status + batch lot). Web batch-sheet page (BatchSheet.tsx) prints via the browser (Print button) and prints recorded IPT results (§ Express row). All parts live — release (§ lifecycle row), ticket validated field-for-field vs the plant PDF (order 189170), specify-packouts with over-packout warning + multi-batch Supplied Qty |
| Complete/close with workflow approvals | ✅ | **Order lifecycle** live: Release (NST→RLS) / Complete (RLS→CMP, records actual batch size + reason) / Close (CMP→CLS), each a mutating endpoint with its own program (`orders.release`/`.complete`/`.close`), invalid-transition guards, and **atomic hash-chained audit** (validated end-to-end on order 189299). **E-signature on Complete now enforced**: password re-auth (+ optional witness) → hash-chained `ESignature`, gated by the `order.complete` secured item; completion is blocked until signed. Multi-step workflow-approval chains ⏸️ with evidence: Workflow/WorkflowDetail/Approval/ApprovalDetail/RoleApprovalDetail/OrdrApprovedAddition/RecipeApprovedAddition are all 0 rows (every workflow/approval view sits over these empty tables); the only Log hit in 15 years is 'Approval Update' — 6 rows in a 21-second window on 2014-02-05 under the vendor's own account (`mar-kov\lex`, installation testing), never used since |
| Material variance analysis; multi-batch | ✅ | **Material variance report live**: `GET /orders/:id/variance` — per material (UI) line planned (`QtyReqd`) vs recorded-actual (`QtyUsed`) with Δ qty / Δ % / Δ cost, each line costed at the **real consumed unit cost** on that order (weighted from its consumption genealogy edges, `Item.PurchasePrice` fallback), plus totals and the yield line (PK planned vs `ActualBatchSize`). Web **Material variance** panel on production orders; gated by its own `orders.variance` program (the report exposes unit costs); yield reported only once Completed (before that ActualBatchSize still holds the planned size). **Multi-batch ⏸️**: verified unused in this install — `Ordr.Parent` is NULL on all 75K orders (the plant sizes each batch to its demand and allocates packouts across batches via Supplied Qty instead, which ERP1's specify-packouts supports); the column is mirrored, creation/roll-up UI deferred per the unused-module rule |
| Full Batch Execution (preweigh, resources, procedure, release, express blend, packaging, reversing, yields, review, scale, auto next/scale) | ✅ | **Guided execution core live** (`orders.execute` program, discovery-proven against executed live orders — see ASSUMPTIONS §Guided batch execution): a **guided execution panel** walks the procedure in ExecOrder sequence on a Released MFBA/MFPP order. Per material line `POST /orders/:id/lines/:lineId/record` records the ACTUAL dispensed qty on the legacy columns (`QtyUsed`, `ExecStatus` NST→CMP; 0 = skipped) and consumes it — specific dispensed lots for a lot-traced item (validated per-item, Σ = actual; forward recall lineage) or FIFO by qty — depleting on-hand + re-rolling the produced lot's real cost (the per-line form of consume-lots, same engine). Instruction lines are check-offs. **Batch additions** (`POST /orders/:id/execution/lines`) append an extra ingredient exactly like legacy did (born executed at actuals, native id). Tolerance (`PercentUnder/Over`) warns, never blocks; re-record refused (fix via audited inventory adjust); RLS re-asserted under the order row lock (no race with complete). `GET /orders/:id/execution` drives the panel (planned vs actual per line + on-hand dispense lot picker + IPT grid). Completion stamps PK `ExecStatus='STD'` (legacy convention). **Order reversal (un-complete) now live**: `POST /orders/:id/reverse` (program `orders.reverse`) reverses a batch ERP1 completed (native orders only; CMP → back to RLS) — un-mints the produced on-hand (kept Lot/Sublot identity, legacy shape), restores every consumed lot from the `source='consumption'` genealogy edges (then deletes them — recall must not trace a reversed batch), clears the produced lot's rolled-up cost, resets executed lines to `NST`/`QtyUsed NULL` (the observed legacy full-reversal shape, order 189797) incl. batch additions, restores the planned `ActualBatchSize`, and writes a reversing **`RVSMFP`** ChangeSet effective-dated to the completion (legacy convention). Guarded by the vendor's 7.17 rule — refused unless the produced stock is exactly as minted (one untouched parcel), never consumed downstream, never shipped; imported legacy completions refused (their InvMovement footprint isn't ERP1-shaped). E-signed via the new operator-tunable **`order.reverse`** secured item (reason+signature default, same fail-safe as `order.complete`); an ERP1 extension — the vendor forbade reversing after Mark Order Complete (UG §6.11.10), see ASSUMPTIONS §Order reversal. Web: a collapsed **"Reverse completion…"** control on completed native production orders (reason + signature + optional witness). Express shipped (see next row). Preweigh / resources / scale integration (incl. auto next/scale) ⏸️ with evidence (2026-07-09): MustPreweigh never set (0 of 505K OrdDetail, 0 RecipeDetail), Ordr.PreweighOnly=0 everywhere (PreweighZone=1 on 56,086 orders is the stamped ParamsOrder default, not usage), MFDispensesTable 0 rows and every preweigh view sits over empty bases, no 'Preweigh' Log program/action in 15 years; Resource is 0 rows install-wide and OrdDetailResource/RecipeDetailResource/ResourceCalendar all 0; WorkstationScale and CustomScaleDriver 0 rows, no scale Log programs — the plant weighed on paper |
| Express Execution; batch testing | ✅ | **Batch testing (IPT results) live**: `POST /orders/:id/ipt-results` records in-process results during execution onto the order's `OrdDetailTest` rows — an **ERP1 extension** (`erp1_result/passed/result_by/result_at`; legacy results were handwritten on the paper ticket) — pass/fail computed against the line's Min/Max (LIMS semantics), tester+time stamped, audited; allowed while Released or Completed. The batch sheet now prints recorded results (blank for hand-recording otherwise); the execution panel has the result grid. **Express execution now live** (`POST /orders/:id/execution/express`, program `orders.execute`): one action records every REMAINING unrecorded procedure line at standard — material lines at planned qty, instructions checked off — consuming stock FIFO oldest-first for every item in ONE locked acquisition (the system-wide lock-order invariant); lot-traced items' FIFO picks are recorded as the dispensed lots (real forward lineage — the express trade-off vs scanning specific lots), cost rolled into the produced lot, shortfalls warned never blocked, lines recorded individually beforehand left untouched. One tx, one audit entry. An **Express…** confirm control on the execution panel |
| Batching order edits (standard + express, rework, over-dispense fixes, publish) | ✅ | **Edit-before-release live**: `POST /orders/:id/edit` (program `orders.edit`, NST orders only) rescales every line to a new batch size from its stored per-unit base (StdQty) and updates header fields (required date, reference), atomic + audited; "Edit order" control on the order detail. **Released-order edit revisions live** (UG §7 — the legacy `OrdrEdit`/`OrdDetailEdit`/`OrdDetailTestEdit` tables are **0-row** in this install, so semantics are native design per the manual, on the mirrored tables): `POST /orders/:id/revisions` (program `orders.revise`) opens a draft on a Released MFBA/MFPP order — snapshots every line (+ IPT test specs, with back-pointers) and flips the order to **EDT** ("Being edited", added to the lifecycle), which blocks execution/complete/packouts/second drafts since they all re-assert RLS under the row lock. Draft ops (all audited): revision comment (required to publish), qty/comment edits on unexecuted material lines, add ingredient/instruction/IPT-with-tests lines (the vendor's failed-IPT fix, §7.2.5 — tests validated against the Test catalog), remove lines (refused for executed / PK / UB lines, lines carrying `OrdDetailCommit` allocations, IPT steps with recorded results). **Publish** (`POST .../draft/publish`, e-signed via the operator-tunable `order.revise` secured item, **pinned to the reviewed draft** — editId + updatedAt content token asserted under the lock, so a swapped or since-edited draft 409s instead of carrying the signature) re-validates every change against the LIVE lines in-tx under the order row lock (executed-since-drafted, vanished, **appeared** — import-drift lines are refused, never silently deleted; removals are marked on the draft, restorable, so it keeps its full baseline; quantity edits floored at the packout-committed qty), snapshots the pre-edit order as **revision 0** at first publish (UG §7.1.8), applies updates/removals/additions (native ids, appended to the procedure), stamps the edit CMP + `Ordr.Revision`=N and returns the order to RLS. **Reject** (UG §7.1.7) → edit REJ, number reused, order untouched. Revision history (UG §6.5.4, rejected excluded) + draft editor + publish/cancel controls on the order page. Legacy data shows `Ordr.Revision`=0/NULL on all 75K orders (module never used at this plant): the vendor's rework-after-packout phase choreography and the Fix-Over-Dispense auto-scale button are folded into the general editor (same mechanics: raise quantities / add lines / new phases) — see ASSUMPTIONS §Order revisions |

## 6. Packaging order processing (UG ch.8–9)
| Feature | Status | Notes |
|---|---|---|
| Create/release/print/complete | ✅ | **Native packaging-order creation now live** — the same `POST /orders` create engine handles an RMPP recipe → `Ordr` Context=`MFPP` (order type derived from the recipe context). Scales UI+PK lines by batch size (StdQty preserves the per-unit base; no IPT/QC lines — packaging carries no in-process tests), born Not-started, flowing into the shared batch-sheet → release → complete (e-signature) → close path. **Guided execution works on MFPP too** (§5): the same per-line record-actuals / batch-addition / variance endpoints + panel handle packaging components (no IPT grid — packaging carries no tests). **Packaging-product lookup (7.22) now live**: the create-order search also surfaces packaged products (`GET /orders/packout-options?q=` over the `ItemPackagedProduct` bindings, by bulk or packout item code) — picking one orders the resolved active packaging recipe, so operators find "the 5-gal packout of E7926" without knowing recipe numbers; batch orders create linked packaging orders directly from their Packouts panel (§5). Package Execution end-lot/setup specifics + reserved-material release ⏸️ with evidence (next row); express ✅ (next row); packaging order edits ✅ (§ Packaging order edits row) |
| Package Express Execution & Package Execution (setup, packaging, resources, instructions, tests, end-lot, reserved-material release) | ⏸️ | **Express execution works on MFPP** ✅ (§5 — same engine: all remaining component lines at standard, FIFO consumption + lineage, one tx); per-line execution, batch additions, variance ✅. **End-lot specifics + reserved-material release ⏸️ with evidence** (2026-07-03): every MFPP OrdDetail in the live data has `Phase` NULL — no packaging recipe/order ever carried an End Lot phase; `LocationVessel` (the vessel/container reservation table behind Return-To-Stock's unreserve) is **0 rows**; the only `ReserveAmount` usage is 45 SH sales orders (unrelated). The plant never scanned/reserved containers or used end-lot choreography — don't rebuild without a user ask (same rule as multi-batch) |
| Packaging order edits | ✅ | Same engine as §5 batching-order revisions (UG §9 defers to §7's mechanics): `POST /orders/:id/revisions` works on Released MFPP orders — component (UI) qty/comment edits, add/remove lines, EDT lock, e-signed publish with the revision-0 snapshot and revision trail. Bulk-use (UB) lines stay locked (their `UseFrom` semantics are undecoded — see ASSUMPTIONS) |

## 7. Controlled substances (UG ch.10)
| Feature | Status | Notes |
|---|---|---|
| Lot/container reconciliation, tare correction, reverse disposal | ⏸️ | Never used at this plant (2026-07-09 evidence): Item.ControlledSubstance is True on 0 of 21,176 items (7,411 False / 13,765 NULL); the Lot reconciliation fields are untouched on all 55,854 lots (DateReconciled / ReconciliationStatus all NULL, ReconciliationFailed on none) and the ReconciliationStatus table is 0 rows; tare correction never used (InvMovement.TareEntered on 0 rows, Location.TareCorrectionRequired on 0 locations); 15 years of Log contain no program/action matching '%Controlled%' or '%Reconcil%'. UG ch.10 wholly unused — deferred per the unused-module rule; don't rebuild without a user ask |

## 8. Resources & maintenance (UG ch.11)
| Feature | Status | Notes |
|---|---|---|
| Rooms, vessels, pails, equipment, scales, abstract resources | ⏸️ | **Never used at this plant**: `Resource` 0 rows and all ten satellite tables (ResourceCalendar/Category/Control/ScheduleGroup(+Detail)/Valve, RecipeDetailResource, OrdDetailResource/Edit/Temp) 0 rows — no recipe or order ever referenced a resource; zero Log rows in 15 years for any Resource/Room/Vessel/Scale/Equip program. Deferred per the unused-module rule |
| Resource labels | ⏸️ | **No data behind it**: labels print for `Resource` records and this install has zero (`Resource` 0 rows, zero Log usage of any resource program in 15 years). Deferred with the resource module |
| Scheduled & unscheduled maintenance, recording, availability, notifications, history | ⏸️ | **Never used**: `MaintDetail` (actual maintenance records) 0 rows, `MaintScheduleGroupItem`/`MaintScheduleItemSpec` 0 rows — only 5 vestigial config rows (3 MaintScheduleGroup + 2 MaintScheduleItem) bound to no resource (`Resource` 0 rows); `ParamsCMS.VesselAndEquipMaintRequired` = False; zero Log rows in 15 years for any Maint program. Deferred per the unused-module rule |
| Scanned maintenance sheets | ⏸️ | **Never used**: no `Document` row references any Maint table (TableName distribution is Ordr/ChangeSet/Waybill/Comment only), `MaintDetail` 0 rows (no maintenance ever recorded to scan a sheet for), and the scan-import config (`WorkstationScandocParams`) is empty. Deferred with the maintenance module |

## 9. Sales & shipping (UG ch.12)
| Feature | Status | Notes |
|---|---|---|
| Shipping Orders (quotes, POS) | ✅ | `Ordr` Context=`SH`. **Native shipping-order creation now live**: `POST /shipping-orders` (program `shipping.create`) creates an `Ordr` Context=`SH` for a **customer** (Entity null; BillTo billed + ShipTo shipped, defaulting to the BillTo — proven against live data) with `OrdDetail` Context=`SH` lines (item, qty, optional sale price/unit/description) and optional carrier (`isShipVia`) / salesman (`isSalesman`) / terms (Terms FK) / customer PO #. Born Not-started, native ids ≥1e9 under the shared id-allocation lock, Owner stamped data-drivenly (our org), atomic hash-chained audit; validates BillTo `isBillTo` + every item + the optional FKs up front. A **New shipping order** form on the Orders page (customer + carrier typeaheads, terms picker, item line editor) with customer/carrier/item/terms pickers gated by `shipping.create`. The created order flows into the shared lifecycle → shipment-lot capture at close → the existing invoice/packing-slip docs. **Shipment-lot capture now live**: closing a shipping order records which finished-good lot(s) + qty shipped (`POST /orders/:id/ship-lots`, program `orders.ship`) into the ERP1-native `shipment_lot` table — the lot→shipment link the legacy CMS never recorded (`OrdDetail.Lot`/`.Sublot` are null on shipment lines). A "slick lot-picker" (`GET /orders/:id/ship-lot-options`) offers, per lot-traced line, the on-hand FG lots to pick from (one click adds an entry); free lot entry is also allowed. Only accepts lots of lot-traced items; depletes the shipped on-hand via the valuation engine (one locked scan), stamps `OrdDetail.QtyUsed`, emits SH movement legs per (parcel draw × line), and mints a native **SH ChangeSet = packing slip** (returned as `packingSlipId`; appears in the Packing Slips browser, id ≥ 1e9, no Waybill); atomic hash-chained audit. **SH line prices now auto-source from the customer's sales price list**: `create` defaults each line price to `SalesPricingService.priceForCustomer(billTo, item, qty)` (the customer's effective price-list version, quantity-break tiered) when no explicit price is given — mirroring how PO lines source from supplier price versions; the create form pre-fills via `GET /shipping-orders/price?customerId&itemId&qty` (prefers the list price over `Item.salesPrice`, drops/re-sources lines on customer change). **Line-level edits now live** (mirroring the PO line editor): add / update / remove a line on a not-started SH order (`POST` / `PATCH` / `DELETE /shipping-orders/:id/lines[/:lineId]`, gated `shipping.create`) — adding a line re-sources the customer's effective-version list price (operator price wins); IDOR-safe, NST-only, refuses removing the last line. SH lines carry no receipts/packaging snapshot (those are PO-only), so no received-quantity guard applies. An **"Edit lines"** panel on the not-started SH order detail (Orders page) edits qty/unit/price in place, removes, or adds an item. Quotes & POS deferred by evidence: `Ordr.IsQuote` = 0 on all 15,739 SH orders ever (no quote context exists either — Ordr contexts are only MFPP/MFBA/SH/PO); POS/counter sales never happened — `TransPayment` 0 rows, `PaymentMethod` only 5 seed rows, and 15 years of Log (667K rows) have no quote/POS/till/payment/complete-sale program |
| Reserve/unreserve containers, shipping assemblies | ⬜ | **Build queued** — heavily and currently used ('Shipping Assembly' 15,855 Log uses, 1,916 in 2026 YTD; PrintShippingLabel 17,666; 17,664 ASM locations; 12 parcels reserved RIGHT NOW via `Inventory.OrdDetail` on open SH order 189526, physically staged in ASM locations): (1) reserve/unreserve on-hand parcels to an open SH line (set/clear the already-mirrored `Inventory.ordDetailId` under the valuation locking conventions, reusing the ship-lot-options picker); (2) native shipping assemblies (create a Context='ASM' Location, move reserved parcels onto it via the existing transfer + movement emission); (3) at close, pre-fill ship-lot capture from reservations (reserved-first picking), clear on shipment; (4) Reserve/stage panel + assembly label. Scope: parcel-level only — CNT containers have 0 rows in this install (contexts are VSL/SMP/ASM/WHS/ZON/LCN/PAIL) |
| Waybills, invoices | ✅ | **Customer invoices + packing slips** done. Invoices = `Trans`(CI)/`TransDetail` (21,954); packing slip = the SH `ChangeSet` PK → `ChangeSetShipment`→`Waybill` (17,784). Both have a browser + print-faithful document, reconstructed field-for-field vs the real PDFs (Invoice N00126742 = $166.80; Packing Slip 84768). Shared party/address resolver (`AddressReference`→`Address`). **Supplier bills (AP) now also done** — `Bill`/`BillDetail` (4,495/9,001) mirrored + imported; browser + print-faithful Supplier Invoice (`GET /bills`, `/bills/:id`, program `sales.bills`) with supplier address, lines resolved via `BillDetail.OrdDetail`→item (+ landed cost), totals = Σ InventoryValue. **Native invoice GENERATION now live (2026-07-04)**: `POST /invoices` (program `sales.invoice`) bills a shipping order's shipped-but-not-yet-invoiced quantities — legacy invoices per shipment event (2,861 orders carry several CI invoices), so per line it bills `QtyUsed` minus what prior non-reversed CI invoices already covered (reversal pairs excluded); header (bill-to/owner/salesman/currency/customer-PO) copies the order exactly like the 22K live rows; invoice number continues the plant's `N`+8-digit sequence (regex-guarded max, shared with imported rows); `TransDetail` rows keep the ORDER context ('SH', live convention) and the line's sale price; taxes from the TaxRule engine + freight param. Atomic under the Ordr row lock → id-alloc lock (no double-billing race), audited; renders in the existing invoice browser/print doc. `shipLots` now also stamps `OrdDetail.QtyUsed` (the legacy shipped-so-far convention) so native shipments are invoiceable and the backorder math works. A **Generate invoice…** control on shipped SH orders. 3rd-party shipping integration ⏸️ (see dedicated row below): never used at this plant — `Entity.ShipIntegration` NULL on all 929 entities, all 17,910 `WaybillTrackingNumber` rows are None (no carrier system ever wrote back), and the Waybill program logged 70,454 manual SaveChanges vs 3 ProcessOutstandingRequests in 15 years |
| Warehouse/lab transfers, bill-and-hold, returns/credits | ⬜ | **Builds queued**: (a) **warehouse transfers** — ACTIVE (Trans Context='TI' = 182 transfer invoices, every year 2020–2026, latest 2026-07-08; mechanism verified: plain SH order with ShipTo=warehouse entity + BillTo=own site invoiced as a T-sequence TI; consigned WHS locations owned by the warehouse entity hold live stock): extend the invoice generator to mint TI when ShipTo is IsWarehouse, relocate shipped parcels into the destination's consigned location instead of depleting, list TI in the invoice browser (detail already renders it — INVOICE_CONTEXTS=['CI','TI']); (b) **returns/credits** — ACTIVE (96 negative-QtyReqd SH return lines, latest 2026-04-01; credits = 2,343 CI reversal pairs via ReversedTrans, 330 in 2026 YTD and growing): negative-qty return lines end-to-end (relax @IsPositive, returned lots INTO stock with RVSSH-style legs, negative billing = credit) + a native reverse-invoice action (new Trans pointing at the original via ReversedTrans — the billed-qty math already understands pairs). Lab transfers ⏸️ (0 IsLab entities install-wide — structurally impossible/never used); bill-and-hold ⏸️ (no bill-and-hold column anywhere in the legacy schema, no Hold program in 15y, Ordr.NoBill never set on any of 15,739 SH orders) |
| Till reconciliation | ⏸️ | Never used: till reconciliation reconciles POS cash-sale payments, and this install recorded zero — `TransPayment` 0 rows ever (the `TillReconciliationReport` view reads it via TransTotalPayments), `PaymentMethod` only 5 seed rows, and 15 years of Log (667K rows) have no till/payment/cash/complete-sale program. Revisit only if POS is ever adopted |
| 3rd-party shipping software integration | ⏸️ | Never configured, never wrote back, never polled: `Entity.ShipIntegration`/`ShipIntegrationReference` NULL on all 929 entities; all 17,910 `WaybillTrackingNumber` rows are None (zero carrier write-backs in 15 years); the integration hook ran 3 times ever vs 70,454 manual waybill saves (Log Program=Waybill by Action); `Waybill.FreightService` is hand-entered (16,530 of 17,495 = 'None'). Shipping is done manually with the carrier service typed on the waybill; nothing to integrate |

## 10. Planning (UG ch.13–14)
| Feature | Status | Notes |
|---|---|---|
| Inventory supply & demand (allocate supply/demand, source/demand tables) | ✅ | **Read-only rebuild of the legacy "Allocate Demand" tool (UG §13.3), 2026-07-04**: `GET /planning/supply-demand?itemId=` (+ item typeahead, program `planning.supplyDemand`) organizes ALL supply and demand for one item — Sources (nettable WHS available stock with held/rejected split, open PO lines at remaining qty, open MFBA/MFPP orders' PK output) with allocated/balance from `OrdDetailCommit`; All-Demand (open SH lines + MF UI ingredient lines: required/committed/balance, what the demanding order produces); the allocation edges linking them. A **Supply & Demand tab** on the Planning page: item picker, linked tables — selecting a source highlights the orders it supplies and vice versa (the vendor's five-table interaction, read-only). Same openness/nettable/release rules as the recalc engine. Allocation EDITING intentionally omitted: the only allocations this install ever recorded are packaging bulk commitments, which ERP1 edits on the batch order's Packouts panel (the UI says so) |
| MRP (recalculate plan trace, plan-tracing viewer, create PO from plan, short-inventory viewer) | ✅ | **Slice 1 live (2026-07-03)**: `PlanTrace` mirrored (2,825 live rows — the legacy nightly recalc IS used at this plant) + imported. The recalc rewrites the whole table with fresh ids and is never change-logged, so the import engine gained **replaceStale** semantics: PlanTrace re-copies wholesale on every sync and vanished legacy-range rows are pruned (native rows untouched — the invariant holds). **Plan Tracing viewer** (`GET /planning/trace`, program `planning.trace`): requirements in plan sequence with item decoration, reference filter (AVAIL/Hold/Expired/MF#/PO#/Short/Negative), the vendor's expedite rule (available later than today + required → pink flag), last-recalculated stamp. **Short Inventory viewer** (`GET /planning/short`, program `planning.short`): one line per item + required-manufacturer + required-lot over Short/Negative rows — total short qty, live on-hand, earliest required / latest available / earliest order-by, preferred supplier. Web **Planning** page (two tabs). **Slice 2 live (2026-07-03) — the NATIVE Recalculate Plan Trace engine** (`POST /planning/recalculate`, program `planning.recalculate`): the vendor §14.1 fill order over ERP1's own data — available stock (own site → consigned, referenced by the owning entity's code) → quarantined `Hold` / `Expired` (assumed approved, Retest status) / pinned-sublot `Rejected` → open MF orders' PK lines (`MF#n`, `+` when overdue) → open PO lines (`PO#n`, earliest promise date, manufacturer-pinned) → `Short` + requirement explosion through the ACTIVE costing recipe (per-1-lb UI lines × short qty, root order carried through all levels, MFLevel = the item's deepest wave, 25-wave cycle guard) → `Short` for purchased items. Demand = open SH lines + MF UI lines (QtyReqd − QtyUsed) in released-then-date order; min-stock (new **`ItemEntity` mirror**, ST rows = the site's MinimumStock/LeadTime/TestingLeadTime — Item has no such columns here) runs after orders (orders win the stock — verified against the live legacy plan). Native rows id ≥ 1e9 written atomically under the native-id lock (legacy rows untouched); viewers switch via app setting `planning.source` (flipped by recalc, `?source=` override for side-by-side); audited; web Recalculate button + engine badge. `Ordr.LeadTime` + `Item.CostingRecipe` now mirrored. **Create-PO-from-plan live (2026-07-04, §14.2.1)**: `POST /planning/create-po` (program `planning.createPo`) — selected Short/Negative lines (checkboxes on the Plan Tracing grid; sublot-pinned lines excluded) must share one Item + Required-Manufacturer; supplier must price the combination on its CURRENT price version (generic or manufacturer-specific detail); multiple qualifying suppliers → ranked options round-trip (preferred first, nothing created) and the client re-posts with the choice; one PO via the existing purchasing.create engine (tier price, packaging snapshot, audit) with qty = sum, DateRequired = earliest, and the required manufacturer stamped on the PO line so the next recalc matches the supply to the pinned demand |
| Capacity Planning (post-guide) | ⏸️ | `CapacityPlan` 0 rows |

## 11. LIMS / QA (UG ch.15)
| Feature | Status | Notes |
|---|---|---|
| Tests & test groups; testing requirements by item | 🟡 | `ItemTest` (item testing requirements, 13.4K rows) now modeled + imported and consumed by native order creation (product OnProduction tests → order QC specs). **ItemTest viewer now live**: `ItemTestsService` / `GET /item-tests/item-options`, `GET /item-tests/:itemId` (program `qa.itemTests`) + an **Item Tests** page — search an item, see its QC tests with formatted specifications (min/max/spec), target, group, grade, and the stages they apply to (Receipt/Production/Retest). The same `ItemTest` rows drive order QC specs + the CofA. **Editable now**: add / update / remove an item's test requirements (`POST` / `PATCH` / `DELETE /item-tests/:itemId/tests[/:testId]`, program `qa.itemTestsEdit` — separate from the read program) — partial updates (only supplied fields change; null clears a value), IDOR-safe, atomic + audited, new rows get native ids (≥1e9, re-import-safe) and the next line. The **Item Tests** page now has per-row Edit / remove + an Add-test form (min/max/target/spec/qualifier/group/grade/comment + Receipt/Production/Retest stage checkboxes, with a test-name datalist). **`Test`/`TestGroup` master catalog now mirrored + imported** (35 + 4 rows; sampling defaults, result type NUM/BOOL + precision, method/spec text) and the editor's **test-name picker is catalog-backed**: `GET /item-tests/test-name-options` offers the catalog (with description/result-type/unit) first, merged with any ad-hoc names existing only on ItemTest rows (legacy references the catalog by name, no FK — ad-hoc names stay valid), deduped case-insensitively; the web datalist shows the catalog descriptions. **Remaining build (queued): Test-catalog admin** — the 'deferred until needed' call was stale: legacy 'Test Update' IS the Test-catalog editor (request XML shows Object="Test" Insert/Delete), 76 uses from 2014 through 2025-12-23 and still adding tests ('IR UPCONVERT' inserted 2025-12-23). CRUD on the Test master (name, description, result type NUM/BOOL, precision, test group, prototype flag), new program qa.testCatalogEdit, audited, native ids ≥1e9; the picker already merges catalog+ad-hoc so new rows appear automatically. TestGroup stays import-only (no TestGroup-editor program ever logged in 12.5y; 4 stable rows) |
| Sampling (sample sets, labels, sampling, IPT, retesting) | ⬜ | IPT ✅ (OrdDetailTest order QC specs created from ItemTest OnProduction at native order creation + native IPT result entry — an ERP1 extension; legacy had no result column). Retesting ⏸️ by evidence: ItemTest.OnRetest=1 on 0 of 13,524 rows; Item.RetestPeriod nonzero on 0 of 21,176 items; Release.Status ever recorded = Approved 54,751 / Hold 26,449 / Rejected 68 only (no retest state); TestGroup.RetestSamplingMethod=None on all 4 groups. **Build queued (the plant's daily QC loop, top of queue)**: native sample-set/Release creation — SampleSet 25,416 rows still growing (Release #84348 dispositioned 2026-07-08), auto-created SERVER-SIDE inside legacy receipt/batch txns (zero Log requests ever insert one; no %Sampl% program exists), and ERP1 has NO release.create anywhere today. At the three seams ERP1 owns (purchasing.receive, misc-receipt, order completion): create the Release row (Hold when the item has ItemTests), mint a native sampleSetId, pre-create LocationSampleTest rows from ItemTest specs (the shape ReleasesService.enterResults expects), emit the configured 'New Sample set' notification (rule #10 — the only configured QA code left unwired), printable sample label (PrintContainerLabel 25,434 uses, live daily) |
| Sample receiving; stability testing; custom sampling | ⏸️ | Never used in this install (verified live 2026-07-09): SampleReceipt 0 rows, StabilityTestReceipt 0 rows, SampleSet.IsStability=false on all 25,416 sets, TestSchedule/LimsTestGroup/TestGrouping all 0 rows, no %Sampl%/%Stabil% program in 12.5y of retained Log; all 4 TestGroups use plain LOT/BATCH methods (no custom sampling ever configured). Stability/sample-receipt notification codes catalogued as unwired with this evidence |
| Enter test results; disposition sublot; at-risk | 🟡 | `Release` (80,400 rows) mirrored + imported; **QA disposition** surfaced per lot on the Lot Trace. **Result entry live**: `GET/POST /releases/:id/tests` (program `qa.results`) lists a sample set's `LocationSampleTest` rows vs the product's `ItemTest` spec and records results (Pass/fail auto-computed against spec, tester+time stamped, audited) — a "Enter test results" grid on the Lot Trace focus lot. **Changing disposition live**: `POST /releases/:id/disposition` (program `qa.disposition`) sets status/grade/purity/expiry, **e-signed** via the operator-configurable `release.disposition` secured item (re-auth + optional witness + hash-chained signature ledger), atomic audit. **Now also the approval engine's first enforcement** (see §0 Approvals & Workflow): a request-only group's disposition is held PENDING until a qualified approver approves/rejects it. So sampling→results→disposition→CofA is native end-to-end for EXISTING sample sets. At-risk ⏸️ by evidence: Container.AtRisk=1 on 0 of 25,416 containers; Release statuses ever recorded = Approved 54,751 / Hold 26,449 / Rejected 68 (81,268 rows, no at-risk state); no at-risk program in 12.5y of Log. Creating NEW sample sets = the §Sampling build above (this row flips ✅ when it lands) |
| Print Certificate of Analysis; auto expiry; reduced testing; QA notifications | 🟡 | **CofA now live**: `ReleaseCofA` (54K) header + `LocationSampleTest` (69K results, sql_variant Result stored as text) mirrored + imported. Certificates browser + **print-faithful CofA document** (`GET /cofa` / `/cofa/:releaseId`, program `qa.cofa`) — reconstructs Test \| Specification \| Result \| Pass by joining recorded results (via `Release.SampleSet`) to the product's `ItemTest` specs (matched by test name), with the QA disposition (grade/status/released-by/date) from `Release`. QA notifications ✅: ERP1 natively emits 'Release Sublot', 'Tests Completed' and 'Testing Required' at the vendor seams; 'New Sample set' (rule #10, the only configured QA code left) rides the §Sampling build; the remaining unwired QA codes (QA Cancel, Reduce Testing Toggled, 3 stability codes) belong to never-used features. Auto expiry ⏸️ by evidence — zero expiry data install-wide: Release.ExpiryDate NULL on all 81,268 rows, Container.ExpiryDate NULL on all 25,416, ItemManufacturer.ExpiryDate 0 rows, Item.RetestPeriod nonzero on 0 items, 13,775/21,176 items explicitly flagged NoExpiry. Reduced testing ⏸️: Lot.ReduceTesting=1 on 0 rows ever; code not configured. Row flips ✅/⏸️ clean once the §Sampling build lands |

## 12. Documents (UG ch.16)
| Feature | Status | Notes |
|---|---|---|
| Scan/import batch records, CofA, MSDS/SDS, maintenance, waybills | ⏸️ | **Scan-import never configured or used**: `WorkstationScandocParams` (the per-workstation scan pipeline config) 0 rows; zero Log rows in 15 years for any Scan/Document program; `Document` (63,649) carries NO file content anywhere (`Data` NULL install-wide) — 63,602 rows are print-registry pointers for system-GENERATED docs (27,844 batching orders, 18,243 packing-slip ChangeSets, 17,517 waybills), which ERP1's print-faithful documents already cover; zero batch records/CofA/MSDS ever scanned (`Documentation` = the UDT data dictionary; `EntityDocument` = customer doc-distribution flags, not scans). Deferred per the unused-module rule |
| Attach documents to comments; document viewers | ⏸️ | **One abandoned burst, no stored bytes**: `Comment` 48 rows / 54 Log executions in 15 years (Dec 2018–Jul 2022, dead 4 years); 47 attachments total (46 PDF + 1 PNG) — 46 are Feb–Jul 2022 Xerox scans of BOLs/pick lists clipped to Waybill/Ordr comments; `Document.Data` is NULL install-wide, so the only surviving copies are base64 blobs inside Log request XML (e.g. Log 441502). Deferred per the unused-module rule; if ever revived, ERP1 would build a native attachment store (nothing migratable from legacy) and the bytes are recoverable from Log XML |

## 13. Accounting & QuickBooks (UG ch.17–18)
| Feature | Status | Notes |
|---|---|---|
| Tax calculations, GL groups/accounts, tax rules | ✅ | **Native rebuild live (2026-07-04)**: `GLGroup`(8)/`GLCode`(11)/`AccountCode`(27)/`GLGroupCode`(70)/`TaxRule`(3) mirrored + imported (all five ARE in the legacy change feed — standard log-driven specs); `Item.Tax2Group/Tax3Group` schema gap closed (Tax1 was already mirrored; Entity had all three). **Accounting masters page** (`GET /accounting/masters` + full CRUD, program `accounting.config`, all mutations audited): GL groups with the per-group account-mapping grid (GL code → external account, one mapping per (group,code), delete guarded by references incl. items using the group), GL codes, account codes, tax rules (native ids ≥1e9 for the int-PK tables). **Tax engine** (`TaxService` + pure `tax-math.ts`, UG §17.4.7 verified): 3 independent levels, per-line resolution exact (customer group, item group) match → blank-item-group fallback → no tax; Rate % + fixed Amount×qty; TaxOnTax compounds on lower-numbered levels; freight taxed via the 'Freight' item group (rate only). 14 unit + 10 integration tests incl. the plant's live 10% rules. `POST /accounting/tax-preview` dry-runs the rules for a customer + hypothetical lines (web card) |
| QuickBooks export + overnight reconciliation | ✅ | Legacy = a live bidirectional COM agent (CMS agent ↔ QB Desktop API) + overnight reconciliation — used for exactly **7 transactions (Dec 2018–Jan 2019)** then abandoned (`QuickBooksTransactions`; invoicing carried on standalone). ERP1 replaces it with a **file-based accounting export** (2026-07-04, see ASSUMPTIONS §13 slice 2): `POST /accounting/export` (program `accounting.export`) renders a date-range double-entry journal as **QuickBooks Desktop IIF** or a **CSV journal** covering the legacy agent's transaction kinds — invoices (AR ↔ per-GLGroup Income + tax + freight), PO receipts as BILLs (AP ↔ Asset at the PO line price, **by-package prices divided per stock unit**, reversed receipts netted out / counter-entered across periods), MISC receipts, native COUNT adjustments (delta from the atomic audit trail × lot cost), native builds (consumption genealogy × consumed-lot cost, purchase-price fallback = the valuation roll-up basis). Accounts resolve Item.GLGroup → GLGroupCode → AccountCode; header accounts (AR/AP/tax/freight/fallback) are app settings; unmapped values book to the fallback account with warnings (preview + response + run ledger). Every line rounded to cents, headers pinned to document totals, unbalanced files refused; CSV formula-injection guarded. Preview endpoint + `accounting_export_run` ledger + audit per download; **Accounting export card** (range/kinds/format, preview, download, run history). Live QB reconciliation ⏸️ (needs a QB Desktop COM session; plant abandoned it — the run ledger is the operational replacement) |
| Cost categories (Labor, Material, Packaging, Burden, Other) | ⏸️ | **No data behind it in this install**: zero Labor/Burden columns anywhere in the legacy DB (verified via INFORMATION_SCHEMA sweep), no cost-category tables (`CostChange`/`CostChangeDetail` 0-row); costing here is item-level actual/standard cost (`OrdrItemCost` 273K rows records per-order standard/replacement/actual). Deferred per the unused-module rule |

## 14. Configuration (UG ch.19)
| Feature | Status | Notes |
|---|---|---|
| Entity/installation/site config | ✅ | Entity IsCMS/IsInstallation/IsSite — the fixed 3-row config chain (CMS→CMSINSTALL→CMSSITE, exactly one of each) imports and is consumed live (notification owner-walk; owner/site resolution in purchasing/planning/shipping via resolveOwnerEntityId); entity editing in the Entities admin. Create-config-entity flow ⏸️ by evidence: the legacy CMS config program last ran 2016-08-31 (84 uses total; Entity Update 2 ops, last 2014) — single-site install, hierarchy static for a decade |
| Config tabs (general, batch-exec, host, inventory, mail, order, print, purchase-receipt, recipe-manager, user) | ✅ | Typed **settings registry** ([settings-registry.ts](apps/api/src/settings/settings-registry.ts)) + `GET /settings/registry` + tabbed `/configuration` page (program `admin.config`), grouped like the legacy tabs. Only LIVE keys are registered (everything shown is read by code): company/document header, batch-sheet grams threshold, **yield tolerance** (wired into complete() warnings — legacy live 5%), receiving/production locations, **reweigh threshold**, **manufacturer-lot-required receiving policy** (legacy ran False; ERP1 defaults true), full Mail tab (smtp.\*/notifications.\*), accounting export accounts, plan source, **security policy wired into auth** (password min length + lockout count/duration — legacy ParamsUser fields, all unset live). Typed writes validated; read-only system keys refused. Legacy Params* fields with no ERP1 behavior are intentionally not mirrored — load-bearing values (lot-code yyMMdd+3, recipe version .NN, prefixes) are verified hardcoded conventions |
| Document logo/branding; bins, location groups, storage rules, units, zones | 🟡 | Units ✅ (master units editor). **Build queued: company-logo upload** — the logo IS used, contra the earlier defer lean: every legacy customer-facing document embeds the company logo image (17,928/17,928 printed invoices carry an identical 28,250-byte Logo blob; PackingSlipReport 17,835 and OrderConfirmationReport 15,739 have the same column; Report.UseLogo=1 on 25 of 108 report definitions) while ERP1 prints only the configurable TEXT company header → an image app-setting (data-URI, size-capped) on the Configuration page, rendered in the shared header of the print-faithful templates (invoice, packing slip, PO, CofA). Rest re-verified 2026-07-09 and stays ⏸️: bins (Bin Update 7 ops ever, last 2020-09-25), location groups (LocationGroup 0 rows), zones (3 static ZON Location rows), storage rules (ValidateStorageRules=False live, never enforced) |
| Workstation & agent config; scheduled procedures; barcode scanners; licensing | ⏸️ | `Workstation*` all 0 rows (desktop-client feature). `Job` has 3 rows: 2 QuickBooks exports (superseded by the §13 IIF/CSV export; see OPEN_QUESTIONS re disabling them at cutover) + a waybill-notify job that never ran. Licensing is a vendor-desktop artifact |

## 15. Multi-language (UG ch.20)
| Feature | Status | Notes |
|---|---|---|
| Static UI text translation | ⏸️ | Verified live 2026-07-08: ALL 4,305 `Vocabulary` rows are LANGUAGE 1033 (en-US) — the table is only the desktop client's English string cache (log messages, SDS headings, field labels), never a translation; no `Entity.Language` is set anywhere (0 rows). The plant runs single-language; nothing to migrate |
| Translatable user data (items, recipes, libraries, order edits) | ✅ | `Item.AltDescription` (7,379 items carry one) is mirrored and surfaced (item data + §18 viewer columns); per-line `Description` overrides are mirrored on OrdDetail/RecipeDetail. That is the full extent of alt-language user data in this install |

## 16. Security (UG ch.21) — *also tracked under §0 foundations*
| Feature | Status | Notes |
|---|---|---|
| Users, roles, secured items, response levels | ✅ | Users admin (create + status + group membership) + Roles admin (create + program grants) + Secured-item admin (response levels + perform/witness grants) all live; see §0. **Row-segregation ⏸️ by evidence**: legacy defines 6 `SecurityGroup` codes but zero data rows ever carried one (Item 21,176, Ordr 75,948, Recipe 19,104, StorageRule 9 — all 'None'; Notification 14 — all '*') — never used in this install. **Secured-item coverage audit done**: of 80 legacy secured items only 12 ever fired (285,985 responses, Dec 2018→Jul 2026); the two with signature volume (OrderEditQtyExceeded 12,909, YieldOutsideTolerance 4,602) map onto ERP1's stricter always-on `order.revise`/`order.complete` gates |
| Electronic signatures / witness, approvals, workflow | ✅ | E-signature capture (re-auth + optional witness → hash-chained ledger) live on **all five** secured-item gates (order complete / reverse / revision publish, QA disposition, recipe publish), driven by operator-tunable response levels; per-user-group approval policy + the reusable blocking-approval engine uniformly adopted across all four consumers (see §0). Witness supported though the plant never used it (0 witness co-signs in 285,985 legacy secured-item responses). **Multi-step approval/workflow chains ⏸️ by evidence**: all 10 legacy chain tables are 0 rows and the only 'Approval Update' log entries ever are 6 rows from a single 2014 vendor setup session — never used. TOTP signing factor arrives with the Auth MFA build (§0) |

## 17. Notifications (UG ch.22)
| Feature | Status | Notes |
|---|---|---|
| Configurable email notifications (containers, items, lots/sublots, orders, planning, receipts, resources, workflows) | ✅ | `Notification`/`NotificationDetail`/`EmailSent` mirrored + imported (never-logged wholesale copies); rule engine (exact security-group → `*` fallback, Send To + per-owner detail walk + contextual placer, `@Field`/`@Table` queue-time HTML rendering with `notifications.baseUrl` deep links); rules CRUD page (`notifications.config`) + e-mail log viewer + requeue; SMTP dispatcher (nodemailer behind a `MailTransport` seam, `smtp.*`/`notifications.enabled` settings or `SMTP_URL` env, 60 s in-API poller, advisory-lock guarded, 5-attempt park, **legacy history rows never dispatched** — all 516 imported rows were stuck 'Not sent' since 2022, the plant's Database Mail never worked). 15 codes wired natively: MFO Created (the only kind this install ever fired) + Released + Complete + Order Edit Publish, Purchase/Misc receipt + both reversals, New Item, Reweigh Outside Threshold (`inventory.reweighThreshold`, live value 5%), Release Sublot, Tests Completed, and post-recalc Inventory Short / Expedite / Testing Required `@Table` summaries. Remaining catalog codes configurable but ⏸️ with evidence per code (workflows 0-row, no waybill/stability/resource/label/PPE/sample-receipt modules, zero reconciliation tolerances, no native sample-set creation) |
| `EmailNotification` ad-hoc e-mail templates | ⏸️ | 0 rows in this install — table not mirrored |

## 18. Viewers & set viewers (UG ch.23)

Legacy set viewers have NO config tables — they're client-defined grids over vendor SQL views. ERP1 instead ships a **declarative viewer platform**: a per-viewer definition (columns/filters/query, `apps/api/src/viewers/viewer-registry.ts`) served by ONE generic endpoint (`GET /viewers/:id/rows` + full-set CSV `GET /viewers/:id/export`, per-viewer program) and ONE generic web grid (`/viewers/:id`). Scope = the plant's ACTUAL working set, ranked by legacy `Log` usage counts (update-side; reads don't log — relative signal only). `InvMovement`/`InvMovementDtl` are now mirrored (609K + 972K rows; never change-logged → append-only sync top-up).

| Feature | Status | Notes |
|---|---|---|
| Viewer platform (declarative config → generic grid + CSV export) | ✅ | Registry + generic executor (paged/sorted/searched server-side; SQL fragments are code constants, values bound; sort via column whitelist); one program per viewer (`viewers.*`, seeded); full-set CSV export with formula-injection guard; `/viewers` index filtered by the user's programs |
| Shipment Detail Set Viewer (usage 396) | ✅ | `shipment-detail`: SH movement legs × order/pricing/waybill/invoice; legacy by-package price math; date-range params |
| Open Shipping Order Detail Set Viewer (290) | ✅ | `open-shipping-order-detail`: open SH lines, balance/committed/uncommitted (positive `OrdDetailCommit` edges; encrypted `GetUncommittedQty` reconstructed as balance−committed, floored 0), value math. Pkg count uses the line's own `OrdDetailPricing.QtyPerEntityQty` (legacy used unmirrored `ItemUnit.BaseQty`) |
| Open Manufacturing Order Detail Set Viewer (153) | ✅ | `open-mf-order-detail`: open UI/PK lines of MFBA/MFPK/MFPP with the legacy sign convention; `BasePriority`/`WeighRule`/`MustPreweigh`/pinned-sublot columns dropped (0-use, verified live) |
| Inventory Movement Set Viewer (153) | ✅ | `inventory-movement` over the new mirror; default filter = legacy `InventoryMovements` view (stock legs MK/MKCA/US/USCA/ADJ/SCRAP), WIP/all toggles; movement-type + date params. `LogDate`/`BatchNbr`/`Division`/customer/`TransferCan` columns dropped (legacy `Log` not mirrored; rest 0-use). **Native emission shipped 2026-07-08**: every ERP1 inventory writer posts legs in-tx (ASSUMPTIONS §20) — the viewer keeps gaining data after cutover |
| Purchase History Set Viewer (61) | ✅ | `purchase-history` (= legacy `PurchasedItem` view): all PO lines with supplier-facing qty/unit/price conversions; open/closed + date params. Currency conversion dropped (single-currency install, verified); `OrdDetail.Manufacturer` dropped (0 rows) |
| Batching Order Set Viewer (44) | ✅ | `batching-order`: MFBA/MFMB orders with PK product, made qty, batch size; status param. Zones/workflow/`FullyDispensed`/yield-memo columns dropped (features ⏸️ or 0-row) |
| Where Used Set Viewer (21) | ✅ | `where-used`: active recipes' UI/UAI lines per output item with per-yield-unit qty; ingredient + published filters; RMPP packaging-prototype join. Manufacturer-authorization columns dropped (0-row pinning) |
| Inventory At Date (Inventory viewer 21 / Inventory Cost 15) | ✅ | `inventory-at-date`: encrypted `GetInventoryAtDate` **reconstructed and validated live** — Σ non-WIP movement legs ≤ date reproduces Qty/ActualValue exactly (CTA1184 6240.1376 ✓). StandardValue dropped (0 on every row in this install); ReplacementValue dropped (needs the vendor's current-cost lookup, not derivable) |
| Complete Manufacturing Orders Set Viewer (14) | ✅ | `complete-mf-orders`: CMP/CLS/REJ orders with made qty, first sublot, packout→bulk lineage via commit edges, and **actual cost validated 12/12 exact** against encrypted `GetQtyMade` (= Σ MK/MKCA/MKB/MKBCA movement values per order). Standard/replacement cost columns dropped (0/not derivable); multi-batch children columns ⏸️ (`Ordr.Parent` never used) |
| Inventory Cost Set Viewer (current per-sublot cost) | ⏸️ | Backing vendor function `GetInventoryCosts` returns ZERO rows for every item in this install (verified live incl. items with stock) — the legacy viewer renders empty here; at-date value coverage above |
| Set viewers already covered by ERP1 module browsers | ✅ | Open Purchase Order Detail (20) → Purchasing browser + `purchase-history`; Inventory (21) / Global (17) / Status → Inventory browser; Invoice (12) → Invoices; Short Inventory (9) + Plan Tracing (6) → Planning; Entities (4) → Entities; Open/Manufacturing Orders (3) + Shipping Orders (2) → Orders; Test Result (2) → QA results; Item (2) → Items; Generic Log Viewer (1) → Audit; Inventory Adjustment (1) → Inv. Receipts/Audit; Lot (1) → Lot Tracking; Email Sent → Notifications log; Approval Status → approval pages; Purchase/Sales Price Detail → price pages; Recipe → Recipes |
| Never-used set viewers (~35) | ⏸️ | Zero legacy Log usage ever (query over all 15 years of retained Log): Assembled Containers, Attachments, Bin Location, Container Movement, Item Components, Item Manufacturer, License Session, Lock, Log Secured Items, Maintenance Schedule Item, Manufacturing Dispenses/Edits/Packaging Details/Ready to Release, Open Workflow Detail, Outstanding Preweighs, Packaging Orders, Packaged Product Formula Costing, Pending Test, Price Version, Receipts, Release History, Resource + Resource Maintenance History, Scanned Document, Shipments, Shipping Assemblies, Short Inventory dupes, Status Label Required, Sublot, Verify Location, Waybills, WIP — most also belong to modules ⏸️ with evidence (resources, waybills, workflows, stability) |
| Read-only viewers (UG §23.1) | ✅ | Covered by ERP1 pages where the plant ever used them (usage over ALL retained Log 2014→2026): Inventory Browser/History → Inventory browser + `inventory-movement` (Inventory History: 1 use, 2021); Inventory At Date → `inventory-at-date` (0 legacy opens); Trace Parents/Children → Lot Trace (0 uses as standalone viewers; genealogy is lot-level in this install); Generic Log Viewer/Log History → Audit (1 use / 0 uses); View Release/Sublot Status → QA disposition + results pages (0 uses); Shipment Viewer → Packing Slips + `shipment-detail`. Container/Assembly Info + container-level traces: assembly windows dead since 2020-04-30 (2,864 + ~30 uses, none in 6 yrs; no Assembly base table, LocationVessel 0 rows) and Container rows are exactly the 25,416 retained QC sample parcels (1:1 with SampleSet) shown on QA pages — nothing left to build |

## 19. Mobile / handheld (UG "Handheld Functions" — 49 programs)
| Feature | Status | Notes |
|---|---|---|
| Handheld/barcode warehouse ops (all 49 `Handheld Functions` folder programs: consume, move container, container info, verify location, reweigh, packout express/weigh, transfer cans, sanitization, label-printer scans, …) | ⏸️ | **Closed by evidence 2026-07-08**: every one of the 49 programs has ZERO uses in 15 years of retained Log; no Log.Program ever contains "Palm" (the handheld UI's window titles); `Log.Application` only ever 'Chemical Management System'/'None' (no handheld client); every Log workstation is a named PC/laptop; no handheld-side tables exist. This plant ran all warehouse ops (receipts 9,134 / inventory counts 3,129 / waybills 70,738) from desktops — ERP1's web UI is reachable from any mobile browser, and the §18/§5 inventory adjust/move/receipt APIs already back those flows. Barcode-first screens would be a NEW feature, not parity (see ASSUMPTIONS §19) |

---

## Intentionally deferred (0 rows in this install — parity schema kept, UI later)
EDI (`Edi*`) · Visits/visitor management (`Visit*`, `Appointments`, `Badge`) · Harvest/agricultural (`Harvest*`, `LotCorn`) · Capacity Planning (`CapacityPlan`) · in-DB SDS (`SDS*` — the separate SDS tool owns this) · TallySheet reports · most `Custom*` extensions · CTFA/INCI cosmetic naming (`CTFA_*`).
