# FEATURE_PARITY closing sweep — dispositions (2026-07-09)

Evidence-gathered by a 7-agent read-only sweep (repo + live legacy DB). Each
entry dispositions one open FEATURE_PARITY row. Apply the FLIP_DONE/DEFER
note edits directly; BUILD items are queued in HANDOFF.md. Evidence was
verified live on 2026-07-09 — re-verify only if the legacy DB changes
materially before cutover.

## L17 [FLIP_DONE] CI (GitHub Actions) (platform-security)

**Evidence:** GitHub API (repos/mattcomputers-ctrl/ERP1/actions/runs): 115 total runs of workflow 'CI' (.github/workflows/ci.yml); latest run #115 on head_sha 0111be1 (= current origin/main HEAD) status=completed conclusion=success (2026-07-09). Pipeline has 3 jobs: build+typecheck+unit tests; integration (Postgres 16 service container, prisma migrate deploy, schema/migration drift gate via `migrate diff --exit-code`, API integration suite); docker (compose config validation for both compose files + full image build).

**Action:** Replacement note: "**Green on main** (run #115 on head `0111be1`, 2026-07-09; 115 runs to date). Three jobs: build + typecheck + unit tests; integration (Postgres 16 service, versioned `migrate deploy`, **schema/migration drift gate** `migrate diff --exit-code`, full API integration suite); Docker (compose config validation + image build). Runs on every push/PR to main."

## L19 [BUILD] Auth — MFA (TOTP), OIDC SSO (platform-security)

**Evidence:** Genuine gap: no TOTP/OIDC library or wiring anywhere in apps/ (grep otplib|speakeasy|openid|passport|oauth over package.json = 0 hits; grep mfaSecret|ssoSubject|totp|oidc over apps/ = 0 hits); only the columns exist (packages/db/prisma/schema.prisma:41-42). Legacy-usage deferral does NOT apply: docs/SCHEMA_REPORT.md:271 says 'There is no MFA/SSO in the legacy model — greenfield in the new system (brief §5)' and docs/ARCHITECTURE.md:33 commits to 'Argon2id + TOTP MFA + OIDC SSO (Entra ID)'. This is a committed new-system security requirement, not a legacy parity item, so zero legacy data cannot defer it.

**Action:** BUILD, size M. Two sub-parts extending apps/api/src/auth (AuthService login flow + session model): (1) TOTP MFA — otplib-based enrollment (provisioning-URI QR from Users admin/profile), second login step when User.mfaSecret is set, recovery codes + admin reset, and expose TOTP as an optional second factor in the ESignatureService re-auth dialog (closes the 'MFA factor' pending on the e-sig row). (2) OIDC SSO — openid-client authorization-code flow (/auth/oidc/start + /callback), link strictly by pre-provisioned User.ssoSubject (no JIT user creation), issuer/clientId/secret via the §14 typed settings registry (same pattern as smtp.*). Entra tenant details are an open question to log in docs/OPEN_QUESTIONS.md.

## L20 [FLIP_DONE] Users / Roles / Programs / Secured Items / Response Levels (platform-security)

**Evidence:** Everything the note lists as live is verified in-repo (Users/Roles/SecuredItems services + admin pages; apps/api/test/integration/http-layer.http.spec.ts exists with the two route-table invariants). The two implicitly-pending sub-parts (per §16 twin row): (a) security-group row-segregation — legacy defines 6 SecurityGroup codes (AUDIT/DEFAULT-ITM/DEFAULT-ORD/Flamm/OTC/PILOT) but ZERO data rows ever assigned one: SELECT SecurityGroup,COUNT(*) grouped on each carrier table → Item 21,176/21,176 'None', Ordr 75,948/75,948 'None', Recipe 19,104/19,104 'None', StorageRule 9/9 'None', Notification 14/14 '*' (wildcard); never used in this install. (b) secured-item coverage audit — legacy SecuredItem has 80 rows but LogSecuredItem (285,985 responses, Dec 2018→Jul 2026) shows only 12 methods EVER fired; only 3 of those are still enabled, and only OrderEditQtyExceeded (12,909) and YieldOutsideTolerance (4,602) ever captured signatures — both subsumed by ERP1's stricter always-on order.revise and order.complete gates; SecuredItemCondition (conditional items) = 0 rows.

**Action:** Flip to ✅. Append to the existing note: "**Remaining sub-parts closed by evidence**: (a) security-group row-segregation ⏸️ — legacy defines 6 `SecurityGroup` codes but zero data rows were ever assigned one (Item 21,176/21,176 = 'None', Ordr 75,948/75,948 = 'None', Recipe 19,104/19,104 = 'None', StorageRule 9/9 = 'None', Notification 14/14 = '*' wildcard) — the mechanism was never used in this install; (b) secured-item coverage audit **done** — of legacy's 80 `SecuredItem`s only 12 ever fired in 285,985 `LogSecuredItem` responses (Dec 2018→Jul 2026), only 3 remain enabled, and the only two with signature volume (OrderEditQtyExceeded 12,909, YieldOutsideTolerance 4,602) are covered by ERP1's stricter always-on `order.revise` / `order.complete` gates; `SecuredItemCondition` = 0 rows (conditional secured items never configured)."

## L21 [FLIP_DONE] Approvals & Workflow chains (platform-security)

**Evidence:** The built portion is complete per the note and verified in-repo (shared ApprovalRequest engine backs all four consumers: order edit, PO lines, SH lines, QA disposition — apps/api/src/approval/, orders.service.ts, releases.service.ts, plus web queues). The one remaining sub-part (multi-step ordered chains) defers on fresh evidence: SELECT COUNT(*) → Workflow=0, WorkflowDetail=0, Approval=0, ApprovalDetail=0, RoleApprovalDetail=0, SecuredItemCondition=0, SignatureMeaning=0, OrdrApprovedAddition=0, OrdrApprovedAdditionEdit=0, RecipeApprovedAddition=0 (all 10 chain/template tables empty). SELECT Program,COUNT(*) FROM Log WHERE Program LIKE '%Workflow%' OR '%Approv%' → single hit 'Approval Update' n=6, all six from 2014-02-05 by vendor account mar-kov\lex (a Mar-Kov setup session that persisted nothing). Plant never used chains in 12+ years of Log.

**Action:** Flip to ✅ (the built engine is the row's substance; the chain sub-part is ⏸️-with-evidence inside the note). Replace the final sentence of the note with: "**Multi-step ordered approval/workflow CHAINS ⏸️ with evidence**: all 10 legacy chain tables are 0 rows in this install (`Workflow`/`WorkflowDetail`/`Approval`/`ApprovalDetail`/`RoleApprovalDetail`/`SecuredItemCondition`/`SignatureMeaning`/`OrdrApprovedAddition`/`OrdrApprovedAdditionEdit`/`RecipeApprovedAddition`), and the only 'Approval Update' entries in 12+ years of Log are 6 rows from a single 2014-02-05 vendor session (`mar-kov\lex`) that persisted nothing — the plant never configured or used chains. Single-step blocking approval (the engine above) is the entire observed need."

## L22 [SPLIT] Supervisor override / approve-on-behalf (platform-security)

**Evidence:** Part flips: the Override capability IS enforced, not just configured — canOverride qualifies a group to enact gated actions directly and to approve any pending request at all six capability checks (apps/api/src/qa/releases.service.ts:118,210,270; apps/api/src/orders/orders.service.ts:772,793; apps/api/src/approval/approval-policy.service.ts:138,158), i.e. asynchronous approve-on-behalf via the queues is live for all four consumers. Legacy at-station usage evidence: of 285,985 LogSecuredItem responses (Dec 2018→Jul 2026), all 17,512 signed responses were self-signed — JOIN Log ON Log shows signer [User] = Log.LoggedInUser in 100% of cases, zero signed-by-another, zero witness co-signs — the legacy in-place credential-swap was never once exercised. Part builds regardless: docs/ARCHITECTURE.md:67 commits to a dedicated in-place elevation flow (brief §5 priority, also in the E2E test list at ARCHITECTURE.md:149), and it becomes genuinely load-bearing in ERP1 because ERP1's gates are stricter than legacy's (signature always required on completion; per-group perform grants), so blocked operators will occur even though they never did in legacy.

**Action:** SPLIT. (a) FLIP the approve-on-behalf/queue half — note text: "Override (`canOverride`) is enforced at every capability check: it qualifies a group to enact gated actions directly and to approve any pending request across all four blocking-approval consumers. Legacy-style at-station credential-swap was never used at the plant (all 17,512 signed secured-item responses in 285,985 were self-signed; zero signed-by-another)." (b) BUILD the brief-§5 in-place elevation flow, size S-M: when a secured action is blocked (missing perform-grant or request-only policy), the signature dialog offers elevation — a privileged user authenticates in place (ESignatureService re-auth already exists), the server verifies their secured-item grant / Override capability, records BOTH identities on the ledger row, and executes the original action in the same transaction (blocked user stays logged in). Composes existing ESignatureService + SecuredItemsService grants + ApprovalPolicyService caps; wire into the five existing gates (order.complete/reverse/revise, release.disposition, recipe.publish) + web dialog.

## L24 [FLIP_DONE] Electronic-signature ledger (platform-security)

**Evidence:** Repo: capture is no longer complete-only — all five secured-item gates sign: order.complete + order.reverse (apps/api/src/orders/orders.service.ts:52,56,1605,2036), order.revise (orders.service.ts:61), release.disposition request+approve (apps/api/src/qa/releases.service.ts:18,23,179), recipe.publish (apps/api/src/manufacturing/recipe-editor.service.ts:33,663); ledger viewer + verify at GET /audit/signatures[/verify]. Legacy: that coverage subsumes ALL signature use the plant ever had — in 285,985 LogSecuredItem responses (Dec 2018→Jul 2026) only OrderEditQtyExceeded (12,909) and YieldOutsideTolerance (4,602) captured signatures (plus 1 ManufacturingReviewIsIncomplete), mapped 1:1 onto ERP1's stricter always-on order.revise and order.complete gates; witness was never used once (0 of 285,985; only 3 of 80 legacy SecuredItems even configured Witness=1) yet ERP1 supports it. The only remaining sub-part, TOTP-as-signature-factor, is part of the Auth MFA build (line 19), not this row.

**Action:** Flip to ✅. Replacement for the final sentence: "**Signing now covers every signature-relevant action**: all five secured-item gates capture (order complete / reverse / revision publish, QA disposition request+approve, recipe publish) — which subsumes all legacy signature use: in 285,985 legacy secured-item responses (Dec 2018→Jul 2026) only OrderEditQtyExceeded (12,909) and YieldOutsideTolerance (4,602) ever captured signatures, both covered by the stricter always-on `order.revise`/`order.complete` gates. Witness co-sign supported though the plant never used it (0 of 285,985 responses). TOTP as an additional signing factor ships with the Auth MFA build (row above)."

## L31 [SPLIT] Items (incl. names, packages, packaging prototypes, services) (masterdata-config)

**Evidence:** Repo: Items module = list/search/context filter (SUNDRY/PP/NAME/PROTOTYPE/PACKAGE)/create + PATCH update API (apps/api/src/master-data/items), but no web edit form and CreateItemDto/UpdateItemDto have no replacedBy — a NAME alias cannot be created functionally. Legacy: Item Update 14,531 ops and Item Name Update 8,787 ops, BOTH live through 2026-07-08 (Item Name Update is the plant's 2nd-busiest master-data program). All 7,375 NAME items carry ReplacedBy → the real item; NAME items are referenced by 0 OrdDetail / 0 RecipeDetail / 0 Lot rows (pure search/label aliases). Services: 0 items with Service/ServiceGroup, no SERVICE context — never used. Packages: Package Update 111 ops (latest 2025-08-06); ItemPkg = 62 rows ALL identical defaults (Tare=0, LabelGroup='LARGE', everything else null) — zero information. Prototypes: Packaging Prototype Update 102 ops (latest 2025-11-06) is a bulk-propagation tool (14,924 RecipeDetail touches — rewrites packing lines when a prototype changes), architecturally replaced by ERP1's read-time packout-binding resolution + recipe editor; PROTOTYPE/PACKAGE items are creatable via the generic create. Item Update also live-writes ItemEntity (9,186 touches — the ST planning knobs MinimumStock/LeadTime/TestingLeadTime that planning reads but nothing natively edits) and ItemPackagedProduct (15,570 touches — packoutOptions enumerates bindings only, and no native itemPackagedProduct.create exists, so a NEW packaged product can't be made orderable natively).

**Action:** BUILD (M), extends the Items master-data module: (1) NAME alias management — create/edit a NAME item with a ReplacedBy target picker, show alias→target in the list, display aliases on the target (the plant's daily new-product flow, 8,787 ops); (2) web item-edit form (PATCH API already exists) incl. the ItemEntity ST planning knobs (min stock / lead time / testing lead time) that legacy Item Update edits and ERP1 planning already consumes; (3) native ItemPackagedProduct binding create (item + prototype + packaged product) so new packaged products become orderable — one small POST reusing the packouts read-time recipe resolution. FLIP/DEFER remainder in note: services never used (0 rows), ItemPkg all-default (62 rows, zero signal), prototype bulk-propagation superseded by read-time binding resolution.

## L32 [DEFER] Item chemical/safety, custom, components, units, tests, kits (masterdata-config)

**Evidence:** ItemTest is DONE: full CRUD editor in QA (apps/api/src/qa/item-tests.controller.ts, programs qa.itemTests/qa.itemTestsEdit, ItemTests.tsx page; ItemTest mirrored). Everything else never carried real data in 15 years: ItemChemical 13,739 rows but every populated safety column is a single auto-default (DisposalGroup='PENDING' on ALL rows, HMIS Health/Fire/React '0', PPE 'A'; DGClass/UNNumber/PackingGroup/Kosher/Halal/SDSCode/ShippingName all 0 rows); its only LogResult writes are row-creation key touches (13,959 ≈ one per row, from Item Update auto-creating the satellite); residual real values (FlashPoint 200, PercentVOC 364, BulkItem 7,151 — redundant with ItemPackagedProduct) are migration-era. ItemCustom 13,739 rows: only [Group] populated (3 static values: 'Packaging Material' 13,038 / 'Raw Material' 700 / 'Bulk' 1), one write per row ever. ItemComponent 0 rows. Kits: ItemKitID 0 rows, 0 items with IsKit=1. ItemUnit 2,265 rows are degenerate (ALL BaseQty=1.0, 2,263/2,265 Weight=1.0, 0 UPCs, 1 distinct PkgType), last written 2020-04-27; ERP1 deliberately sources per-line packaging from OrdDetailPricing instead (documented deviation, ASSUMPTIONS §viewers item 7).

**Action:** Replacement note: 'Item tests ✅ (QA item-tests CRUD editor, programs qa.itemTests/qa.itemTestsEdit). Rest ⏸️ by evidence: ItemChemical (13,739 rows) is auto-created defaults — every safety column a single constant (DisposalGroup=PENDING, HMIS=0, PPE=A; DG/UN/kosher/halal/SDS all 0 rows), only row-creation writes in 15y of Log; ItemCustom likewise (only Group populated, 3 static values, one write/row); ItemComponent 0 rows; kits ItemKitID 0 rows + 0 IsKit items; ItemUnit 2,265 all-default rows (BaseQty=1 everywhere), dead since 2020-04-27 — ERP1 sources per-line packaging from OrdDetailPricing instead.'

## L33 [SPLIT] Suppliers & Manufacturers (masterdata-config)

**Evidence:** Repo: Entities module has list/search/role filter/create/edit with all role flags + inactive/currency/terms/customerType, audited (apps/api/src/master-data/entities); addresses are read-only — create can set a Main name only, no address add/edit endpoint exists. Legacy: Supplier/Manufacturer Update 662 ops, live (latest 2026-07-07), writing Entity (1,392 row-touches) + AddressRef (351 touches — address maintenance is live across ALL party programs: 708 BillTo + 472 ShipTo + 351 Supplier + 44 ShipVia + 14 SalesRep, latest 2026-07-08) + MemoRef (21; entity memos are only 395 of 28,066 MemoRef rows — the rest ride Trans/Ordr). Legacy Address carries 20 columns (AddrLine1-3, City/State/Zip/Country, Contact/Email/Phone/Fax, EmergencyContact) that feed the To/Ship-To blocks on PO and sales documents ERP1 already renders.

**Action:** BUILD (S), shared across rows 33–35: entity address-book CRUD in the Entities module — add/edit/remove Address rows + AddressReference links (Main/Remit/Ship refs) on an entity, with the document-facing fields (name, address lines, city/state/zip/country, contact, phone, email); audited like the existing edit. Role/terms management itself flips to done. Entity memos ⏸️ (395 rows; memo platform belongs to the orders/invoice rows that hold 26K of 28K MemoRef rows).

## L34 [SPLIT] Customers & Ship-Tos (masterdata-config)

**Evidence:** Repo: same Entities module coverage (customer/ship-to flags, edit dialog, role filters); customer→price-list assignment shipped in the PriceLists editor; Entity.Parent (ship-tos under a customer) is mirrored but not exposed in create/edit. Legacy: Bill To Update 1,883 ops (live, latest 2026-07-08) and Ship To Update 827 ops (latest 2026-07-02) writing Entity + AddressRef (708/472 touches — live address maintenance) + EntityDocument + MemoRef. EntityDocument checked: 4,284 rows = exactly 6 document-type rows × 714 entities, EVERY row identical (Order/BillTo/Print all False, Format null) — auto-created boilerplate the client stamps on save, zero configuration ever → defer. 

**Action:** BUILD (S) — the SAME shared entity address-book CRUD as row 33, plus exposing Parent on ship-to create/edit (ship-to hierarchy under a customer; field already mirrored). Everything else flips: list/create/edit/role filters, customer terms/currency/type, price-list assignment (PriceLists editor). EntityDocument ⏸️ with evidence: 4,284 rows all identical default flags (6 types × 714 entities), never configured in 15y.

## L35 [FLIP_DONE] Salesmen, Ship Via (masterdata-config)

**Evidence:** Repo: both roles managed end-to-end in Entities admin (isSalesman/isShipVia on create + edit, Carriers/Salesmen filters); orders reference them via pickers (Ordr.Salesman set on 98% of shipping orders — already wired per ASSUMPTIONS §viewers review note). Legacy usage is thin: Ship Via Update 59 ops ever (latest 2025-08-18), Sales Rep Update 14 (latest 2026-06-05), Salesman Update 8 (last 2019); their only writes beyond Entity are AddressRef touches (44 + 14 + 7 — covered by the shared address editor built under rows 33/34) and EntityFreightService, a 23-row table last written 2023-08-21.

**Action:** Replacement note: 'Managed as Entity role flags (IsSalesman/IsShipVia) in the Entities admin — create + edit + role filters; orders reference both via pickers; audited. Party addresses ride the shared entity address editor (rows 33/34). EntityFreightService ⏸️ (23 rows, last write 2023-08-21; legacy Ship Via Update used 59× in 15y).'

## L36 [FLIP_DONE] Warehouses & Labs (masterdata-config)

**Evidence:** Repo: isWarehouse + isLab wired end-to-end (DTO/service/web checkboxes + role filters) in the Entities admin. Legacy: Warehouse/Lab Update used 19 times EVER, dormant since 2020-10-22; the install has 3 warehouses and 0 IsLab entities (Schema Report §3.1). Role-flag management IS the whole master-data feature; warehouse-specific runtime behavior (transfer targets, receiving/production locations) already ships with the inventory/configuration modules that consume it.

**Action:** Replacement note: 'Both managed as Entity role flags in the Entities admin (create + edit + role filter); isLab wired end-to-end even though this install has 0 lab entities. Legacy Warehouse/Lab Update: 19 uses in 15y, dormant since 2020-10-22 — nothing further to build; warehouse-consuming behavior (receiving/production locations, transfers) lives in the inventory/config modules.'

## L37 [SPLIT] Pricing (price versions & price lists) (masterdata-config)

**Evidence:** The note's two pending items are stale — both shipped: (1) web editor UI = apps/web/src/pages/PriceLists.tsx (route /price-lists, wired in App.tsx) over SalesPricingService; (2) sales-order price sourcing = apps/api/src/sales/shipping.service.ts calls priceForCustomer at SH create (line 121), add-line (320) and a price-quote endpoint (491). GENUINE gap found instead: supplier price-version maintenance. Legacy 'PriceVersion Update' is a top-10 program — 1,998 ops, live daily (latest 2026-07-08 08:58), maintaining the 615 PriceVersion / 15,745 PriceDetail supplier rows that ERP1's PO line-sourcing reads; but ERP1's only PriceVersion/PriceDetail writes are in sales-pricing.service.ts, hard-gated to IsPriceList entities (assertVersionOnList) — supplier pricing is read-only, unmaintainable after cutover.

**Action:** BUILD (M): supplier price-version editor in the purchasing module, reusing the sales-pricing editor pattern — per-supplier effective-dated PriceVersions with per-item PriceDetails (PkgType/EntityQuantity/EntityUnit, supplier's EntityItemCode, MinOrder1/Price1..5 quantity-break tiers, lead time, PriceByPackage), native ids ≥1e9, audited; PO line auto-sourcing already consumes the result unchanged. Flip the stale pending items (sales web editor + SH price sourcing are live).

## L38 [FLIP_DONE] Units, zones, bins, location groups/categories (masterdata-config)

**Evidence:** Units: full master units editor shipped (apps/api/src/master-data/units — list/get/create/update, audited; Units.tsx page, /units route); legacy Unit = 16 rows, Unit Update used 3× (last 2018-12-13). Zones: the install has exactly 3 static ZON-context Location rows (PREWEIGH/BATCHING/PACKAGING); across all 75K orders each zone column holds exactly 1 distinct value (COUNT(DISTINCT ExecutionZone/PreweighZone/PackagingZone) = 1/1/1) and no zone-editor program exists in Log — zones are fixed plumbing, not managed data. Bins: 'Bin Update' used 7× ever, last 2020-09-25, writing 3 Location row-touches; no Bin table. Location groups: LocationGroup = 0 rows.

**Action:** Replacement note: 'Units ✅ (master units editor: CRUD + audit, /units page; legacy Unit Update ran 3× in 15y). Zones ⏸️ — 3 static ZON Location rows (PREWEIGH/BATCHING/PACKAGING), one distinct value per Ordr zone column across 75K orders, no editor program in Log. Bins ⏸️ (Bin Update 7 uses ever, last 2020-09-25, wrote 3 Location touches; no Bin table). Location groups ⏸️ (LocationGroup 0 rows).'

## L43 [FLIP_DONE] Purchase Orders (purchasing-inventory)

**Evidence:** The note's only pending item was 'Approval routing to follow'. Repo: apps/api/src/purchasing/purchasing.controller.ts already serves GET/POST /purchase-orders/line-approvals[/:requestId/approve|reject] (kind po.line.edit on the shared blocking-approval engine, documented in the §0 Approvals row); create/print docs/pickup copy/price-version line sourcing/receiving/recall/line editor all verified present. Multi-step ordered approval chains are tracked (and deferred) on the §0 Approvals row — all 8 legacy Workflow/Approval tables are 0-row.

**Action:** Replace the trailing 'Approval routing to follow' with: 'Approval routing shipped: PO line edits (add/update/remove) run on the shared blocking-approval engine (kind po.line.edit) — a request-only group's change is held PENDING until a qualified approver enacts or rejects it via GET|POST /purchase-orders/line-approvals[/:requestId/approve|reject] (see §0); multi-step ordered approval chains stay deferred on the §0 Approvals row (legacy workflow tables 0-row).' Flip row to ✅.

## L44 [FLIP_DONE] Purchase Receipts (purchasing-inventory)

**Evidence:** Both pending items are stale. Repo: purchasing.service.ts receive() (lines ~877–1018) mints on-hand Inventory per received lot via ValuationService.mintInventory at the configurable receiving-location setting (the 'receiving-location decision' was made — settings registry has the key, wired) and emits InvMovement legs; per-lot unitCost set from the PO line price. 'Return-to-supplier / misc receipts pending' — those are their own rows (45 ⏸️ with evidence, 46 live). Receipt cancellation (UG 3.4.2) = the shipped receipt reversal (POST /inventory/receipts/:changeSetId/reverse, handles PO + MISC).

**Action:** Replace 'On-hand Inventory (qty in a location) deferred — needs a receiving-location decision. Return-to-supplier / misc receipts pending' with: 'On-hand is minted at receiving by the valuation engine (configurable receiving-location setting) with per-lot unit cost from the PO line price + InvMovement legs; receipt cancellation = the §3 receipt reversal (POST /inventory/receipts/:id/reverse). Return-to-supplier (⏸️) and misc receipts (✅) are tracked on their own rows.' Flip row to ✅.

## L46 [FLIP_DONE] Miscellaneous / Create Inventory receipts (purchasing-inventory)

**Evidence:** 'Reverse / adjustment-out pending' is stale. Repo: POST /inventory/receipts/:changeSetId/reverse (program inventory.reverse) reverses posted MISC and PO receipts with a Reverse action on the Inventory Receipts page (row 60's own note confirms it shipped); adjustment-out is the live POST /inventory/adjust (write-on/write-off with required reason, COUNT ChangeSet, movement leg, audit).

**Action:** Replace 'Reverse / adjustment-out pending' with: 'Reverse shipped — POST /inventory/receipts/:id/reverse (program inventory.reverse) with a Reverse action on the page (see §3 reversal row); adjustment-out is the audited inventory adjust.' Flip row to ✅.

## L47 [DEFER] Create Sublot (purchasing-inventory)

**Evidence:** SELECT COUNT(*), COUNT(DISTINCT Lot) FROM Sublot → 55,854 sublots over 55,854 distinct lots; lots with >1 sublot = 0 (GROUP BY Lot HAVING COUNT(*)>1). Log sweep Program LIKE '%Sublot%' over 15 years returns only 'Disposition Sublot' (25,458 — the QA disposition screen, already live in ERP1). Sublot==lot in this install (matches the genealogy finding).

**Action:** Note: '⏸️ Never used: every one of the 55,854 lots has exactly ONE sublot (0 multi-sublot lots in 15 years; the only %Sublot% program in Log is Disposition Sublot 25,458 = QA disposition, live natively). ERP1 mints the 1:1 sublot automatically at receiving / misc receipt / lot-enablement / batch completion — a manual extra-sublot screen would be dead UI.'

## L48 [SPLIT] Purchase price detail sets (purchasing-inventory)

**Evidence:** FLIPS: viewer (GET /purchase-orders/price-details) + PO-line sourcing (GET /purchase-orders/price-detail) verified live in purchasing.controller.ts. BUILDS: the EDITOR — purchasing has zero write endpoints for PriceVersion/PriceDetail, yet the plant maintains them constantly: Log 'PriceVersion Update' = 1,998 uses + ~30 supplier-prefixed variants, steady and current (2023: 521, 2024: 259, 2025: 208, 2026 YTD: 134 — roughly weekly); PriceVersion 615 / PriceDetail 15,745 rows back it.

**Action:** Viewer + line-sourcing flip (already in note). BUILD the supplier price-detail-set editor: create an effective-dated PriceVersion for a supplier + CRUD its PriceDetails (item, package type/qty-per-package/unit, supplier item code, quantity-break tiers MinOrder1–5/Price1–5, lead time, price-by-package), native ids ≥1e9, atomic hash-chained audit; extends the purchasing module + Purchasing page and mirrors the existing SalesPricingService price-list editor (same base tables, Item-keyed instead of InvItem). Size M.

## L53 [FLIP_DONE] Inventory status & sublot expiry (purchasing-inventory)

**Evidence:** Browser + import live (verified: inventory.service.ts list() with status filter + adjust/move actions). Expiry closed by evidence: SELECT COUNT(*) FROM [Release] WHERE ExpiryDate IS NOT NULL → 0 of 80,400 releases (0 future-dated too), and Sublot/Lot carry no expiry column in this schema — the plant never recorded an expiry in 15 years. ERP1 nonetheless models expiry natively (QA disposition sets Release.expiryDate; the planning engine flags Expired/Retest stock), so the capability exists the day the plant starts using it.

**Action:** Note: '✅ Inventory browser + import live (37,934 parcels; status filter, adjust/move/reverse actions). Sublot expiry closed by evidence: the plant never set one — Release.ExpiryDate is NULL on all 80,400 releases and Sublot/Lot have no expiry column in this install. ERP1 models expiry natively anyway (QA disposition sets it, planning flags Expired), so it works if ever adopted; the automatic-expiry job stays tracked on the §11 QA row.' Flip row to ✅.

## L54 [FLIP_DONE] Costing (standard, replacement, actual) — §3 row (purchasing-inventory)

**Evidence:** Actual costing shipped (per-lot unitCost + valuation engine, specific-id + FIFO — validated exact against the encrypted vendor fns: order cost 12/12, at-date exact, per §18 rows). Standard cost never used: Item.StandardCost non-zero on 0 of 21,176 items; OrdrItemCost.UnitStandardCost non-zero on 0 of 273,732 rows; StandardValue = 0 on every movement row. Replacement cost is an auto-maintained latest-cost cache, not an operator feature: Item.ReplacementCost (12,334 items) verified equal to the item's latest PO line price on current orders (CTA1184 1.27=1.27, CTA1074 0.97=0.97, E6352 62.488=62.488, GP1601B 172.50=172.50 …) — ERP1 sources current cost live from the supplier's effective PriceVersion (PO line sourcing + recipe expected-cost rollup), superseding the cache.

**Action:** Note: '✅ Actual costing = per-lot unitCost + valuation engine (specific identification for lot-traced, FIFO for untracked), validated exact vs the encrypted vendor cost functions. Standard cost ⏸️ by evidence: 0/21,176 items carry a StandardCost, 0/273,732 OrdrItemCost rows have UnitStandardCost, StandardValue=0 on every movement. Replacement cost ⏸️ superseded: legacy auto-maintained it as a latest-purchase-price cache (verified Item.ReplacementCost == latest PO line price); ERP1 reads current cost live from the supplier effective price version (PO sourcing, recipe pricing rollup).' Flip row to ✅.

## L55 [FLIP_DONE] Lot-tracking enablement (per item) (purchasing-inventory)

**Evidence:** The note's 'next increments' all shipped: FIFO consumption (valuation engine consume-qty, oldest-first — §3 costing row), batch-consume forward lineage (consume-lots → lot_genealogy source=consumption — §3 genealogy row + guided execution per-line recording), shipment-at-close capture (ship-lots → shipment_lot — §9 row). Verified in apps/api/src/inventory/valuation.service.ts and orders.service.ts.

**Action:** Replace the trailing 'FIFO consumption + forward lineage capture (batch consume, shipment-at-close) are the next increments' with: 'FIFO consumption (valuation engine), batch-consume lineage (consume-lots / per-line execution recording) and shipment-at-close capture (ship-lots → shipment_lot) all shipped — recall is live end-to-end for enabled items.' Flip row to ✅.

## L56 [DEFER] Storage rules (purchasing-inventory)

**Evidence:** StorageRule = 9 rows, but enforcement is OFF in the live install: ParamsPurchaseReceipt.ValidateStorageRules = False (re-verified 2026-07-09), and no storage-rule program appears anywhere in 15 years of Log (Program LIKE '%Storage%' → zero rows). Matches the §14 config row's earlier finding ('never enforced').

**Action:** Note: '⏸️ Never enforced at this plant: 9 StorageRule status-pair rows exist but ParamsPurchaseReceipt.ValidateStorageRules=False live (re-verified 2026-07-09) and zero %Storage% program uses in 15 years of Log. Schema parity only if ever requested.'

## L57 [FLIP_DONE] Adjust / consume / reweigh / remeasure (purchasing-inventory)

**Evidence:** Adjust + consume shipped (already in note; verified in inventory.service.ts / valuation.service.ts). Reweigh/remeasure closed by evidence: SELECT COUNT(*) FROM Log WHERE Program LIKE '%Reweigh%' OR Program LIKE '%Remeasure%' OR Action LIKE '%Reweigh%' OR Action LIKE '%Remeasure%' → 0 rows in 15 years. Functionally covered anyway: ERP1's adjust sets a parcel to its counted/weighed quantity with reason + audit and fires the legacy 'Reweigh Outside Threshold' notification past the configurable threshold (inventory.reweighThreshold, legacy live value 5%) — inventory.service.ts:125-142.

**Action:** Replace 'Reweigh / remeasure pending' with: 'Reweigh/remeasure closed by evidence — ZERO uses in 15 years of Log (no Program or Action ever matched %Reweigh%/%Remeasure%); the audited adjust covers set-to-weighed-qty and fires the legacy Reweigh Outside Threshold notification (configurable, live value 5%).' Flip row to ✅.

## L58 [DEFER] Empty / merge / dispense from containers (purchasing-inventory)

**Evidence:** Zero uses in 15 years: the Log sweep for %Empty% / %Merge% / %Dispense% / %Container% returns ONLY label programs (PrintContainerLabel 25,434, ContainerLabelReprint 21). Containers exist only structurally — 55,763 VSL Location rows ≈ 1:1 with the 55,854 sublots, used as label targets — but no container-level inventory operation was ever run; all stock ops happen at sublot level, which ERP1's (item, sublot, location) parcel model covers.

**Action:** Note: '⏸️ Never used: 15 years of Log contain zero Empty Container / Merge Containers / Dispense From Container operations (the only %Container% programs are label printing). Containers here are VSL Location rows 1:1 with sublots (label targets only); ERP1's sublot-level parcel model covers every stock operation the plant performs.'

## L59 [DEFER] Transfer cans & tanks (create/refill) (purchasing-inventory)

**Evidence:** SELECT COUNT(*) FROM Location WHERE TransferCan = 1 → 0 rows (no transfer can or tank ever created); Log sweep for %Transfer% / %Tank% programs over 15 years → zero (only 172 plain TRNSFR location-transfer records exist, covered by ERP1's transfer).

**Action:** Note: '⏸️ Never used: 0 Location rows with TransferCan=1 and zero %Transfer%/%Tank% program uses in 15 years of Log. Plain location transfers (172 legacy TRNSFR) are covered by the live inventory transfer.'

## L60 [SPLIT] Container & lot disposal + reversal (purchasing-inventory)

**Evidence:** DONE: receipt reversal + batch reversal shipped (verified in repo). DEFERS: container/lot disposal — no disposal program in 15 years of Log (the only %Dispos% program is 'Disposition Sublot' = QA disposition) and no disposal ChangeSet context exists (full distribution: MF 52,813 / MFP 34,860 / SH 17,926 / PO 9,074 / PICK / ASMBLY / SAMPLE / COUNT 1,497 / MISC 424 / RVSMFP 389 / RVSSH 293 / TRNSFR 182 / RS 17 / RVSRS 2 — no disposal context); write-offs run through the audited adjust. BUILDS: shipment reversal — genuinely used and ongoing: RVSSH = 293 reversing change sets, every year 2019–2026 (2019: 47, 2023: 57, 2024: 68, 2025: 32, 2026 YTD: 29).

**Action:** Disposal part defers with the zero-context/zero-program evidence; receipt+batch reversal stay done. BUILD shipment reversal: reverse a closed/shipped SH order — restore shipped on-hand from its shipment_lot rows, delete the shipment lineage, unwind OrdDetail.QtyUsed, write a reversing RVSSH ChangeSet (reverseChangeSet back-pointer, reversed SH movement legs), refuse when already invoiced (or require the invoice handled first), e-signed + audited like the other reversals; UI on the shipped SH order. Extends the orders/sales close flow + valuation engine, patterned exactly on the shipped receipt/batch reversals. Size M. (Plus the trivial follow-up: surface the existing PO-receipt reverse endpoint in the Purchasing UI — currently only the MISC page has the button.)

## L61 [FLIP_DONE] Move & staging (purchasing-inventory)

**Evidence:** Move shipped (POST /inventory/transfer, TRNSFR ChangeSet — legacy did 182 TRNSFR in 15 years). Staging closed by evidence: no %Staging%/%Stage% program ever in Log, and LcnMovement (99,018 rows) contains ONLY Context='CREATE' (98,846 — container-creation records) + 'TRNSFR' (172) — no staging context ever written. LcnMovement is the vendor's internal container-creation/transfer log; ERP1's ChangeSet + hash-chained audit is the equivalent record.

**Action:** Replace 'Staging / LcnMovement detail pending' with: 'Staging closed by evidence — zero %Staging% program uses in 15 years and LcnMovement (99,018 rows) holds only CREATE 98,846 + TRNSFR 172 (no staging context ever); LcnMovement is the vendor's internal container-creation/transfer log, covered by ERP1 ChangeSets + audit.' Flip row to ✅.

## L62 [SPLIT] Inventory count, cycle count, verify location, mark/write-off missing (purchasing-inventory)

**Evidence:** BUILDS: Inventory Count — heavily used and current: Log 'Inventory Count' = 3,129 uses, steady (2020: 361, 2022: 747, 2024: 373, 2025: 319, 2026 YTD: 226); InventoryCount 1,497 headers (Owner/Description/EffectiveDate/Posted/ChangeSet) + InventoryCountDetail 21,047 lines (Item/Sublot/Location/QtyEntered/Qty/QtyAdjust), 1:1 with the 1,497 COUNT ChangeSets ERP1's adjust already writes natively. DEFERS: Cycle Count — 0 Log uses ever; Verify Location — 0 uses; Mark Missing — 1 use in 15 years; Write Off Missing — covered by the audited adjust write-off.

**Action:** BUILD the Inventory Count workflow: mirror + import InventoryCount/InventoryCountDetail; native count sheets — create a count (scoped by location/item/status), capture counted quantities per parcel (book qty vs counted, QtyAdjust computed), post → per-parcel adjustments through the existing adjust/valuation machinery under ONE COUNT ChangeSet, unposted counts editable, atomic + audited; an Inventory Counts page (create, enter, post, history). Extends the inventory module's adjust engine. Size M. Cycle count / verify location / mark-missing sub-parts defer with the zero-or-one-use evidence above.

## L64 [BUILD] Label printing / reprinting (purchasing-inventory)

**Evidence:** Heavily used, ongoing daily: Log PrintContainerLabel = 25,434 uses (2026 YTD: 2,651; ~3,000-3,900/yr) + ContainerLabelReprint 21; PrintShippingLabel = 17,666 (2026 YTD: 1,668) — exactly 1:1 with the 17,665 ASM (assembly/pallet) Location rows, each print stamping a ShipToAddress AddressReference (sampled Log requests). Printing a container label also clears the label-required flag (LocationTools.ResetLabelRequired; LabelRequiredContainer currently 30). LabelFormat = 0 rows — formats were never DB-configured (client-side), so no format designer is needed.

**Action:** BUILD two print-faithful label documents on the existing HTML print-doc pattern (PO/invoice/batch-sheet): (a) container/lot label — item code + description, our lot, manufacturer lot, qty/unit, received/made date, status — printable from a receipt line, inventory parcel, and produced lot (print buttons at PO receiving, misc receipts, inventory rows, batch completion), with a reprint path; (b) shipping label — ship-to address, customer + customer PO, carrier — printable from an SH order. Data is entirely in existing ERP1 tables (Lot/Sublot/Item/Inventory, SH order + address resolver); no format designer (LabelFormat 0 rows). Size M.

## L65 [DEFER] Update container/lot/sublot info (purchasing-inventory)

**Evidence:** Zero uses of Update Container Info / Update Lot / Update Sublot in 15 years of Log: the Program LIKE '%Lot%' sweep returns only 'Disposition Sublot' (25,458) and 'Lot Set Viewer' (1); %Container% returns only label programs. The single lot-editing flow the plant actually uses — Disposition Sublot (status/grade/purity/expiry) — is already live natively as the e-signed QA disposition.

**Action:** Note: '⏸️ Never used: zero Update Container/Lot/Sublot uses in 15 years of Log — the only lot-editing the plant ever does is Disposition Sublot (25,458 uses), which is live natively as the e-signed QA disposition (status/grade/purity/expiry). Field corrections (e.g. a mistyped manufacturer lot) would be a new ask, not parity.'

## L66 [DEFER] Costing (standard, replacement, actual) — §3 duplicate row (`InventoryCost*`) (purchasing-inventory)

**Evidence:** Duplicate of line 54. The InventoryCost* backing is the vendor's INTERNAL actual-cost ledger, replaced by ERP1's validated valuation engine: InventoryCost base table 71,856 rows (QtyMK/UnitCostMK per make ChangeSet) + InventoryUnitCost 68,960 rows (per-sublot QtySOH/ValueSOH/UnitCost), still written by the running legacy system (last row 2026-07-08); InventoryCosts and InventoryCostAtDate are VIEWS, and the current-cost view/vendor fn GetInventoryCosts returns ZERO rows install-wide (verified earlier, §18 row). Standard cost 0 everywhere (see line 54 counts).

**Action:** Mark ⏸️ with note: '⏸️ Duplicate of the costing row above (see it for the full disposition). `InventoryCost*` is the vendor's internal actual-cost ledger (InventoryCost 71,856 / InventoryUnitCost 68,960 rows; InventoryCosts/InventoryCostAtDate are views — the current-cost view returns zero rows install-wide) — replaced by ERP1's per-lot unitCost + valuation engine, validated exact against the encrypted cost functions; standard cost is 0 on every row in this install.'

## L71 [FLIP_DONE] Batching recipes (ingredients, components, coatings, packages, procedures, IPTs, planning) (recipes-batch)

**Evidence:** Repo: full editor lifecycle confirmed live in apps/api/src/manufacturing/recipes.controller.ts + recipe-editor.service.ts (create/edit-header/procedure PUT, clone, delete-draft, publish/verify/active, preview, pricing) and the Recipes web pages. New DB evidence hardens the 'advanced features unused' deferral: SELECT Context, COUNT(*) FROM RecipeDetail GROUP BY Context → UI 105,408 / INSTR 29,147 / PK 19,103 / BA 11,916 / UB 11,908 / IPT 59 / FMT 3 / GRP 1 — no vessel/phase/coating/Use-Group/Adjust/planning-task line contexts exist anywhere in the install. RecipeDetailResource = 0 rows and Resource = 0 rows install-wide (no vessel was ever defined). Recipe-level IPT lines: 59 total, 58 on Inactive recipes, 1 on an active recipe (2AP0400.06); order QC actually flows from ItemTest (13,524 rows) seeded at order creation, and POST /orders copies recipe IPT lines as-is (orders.service.ts line 42), so nothing is lost.

**Action:** Replacement note: "Recipe lifecycle management live (RecipeEditorService, programs recipe.editor/recipe.manager): create draft (native ids ≥1e9, BA+PK structure per live convention), edit draft header + procedure (PUT /recipes/:id/procedure — ordered ingredient/instruction lines at any formula basis, normalized ÷basis on save, per-line audit), clone → next .NN, delete draft (7.18 parity; published immutable). Versioning = .NN suffix + SingleActiveRecipe; per-1-lb normalization (5,650/5,652 active RMBA). UseFrom/TotalVolume columns mirrored + imported. Full Recipes web rebuild (list/detail/draft editor/publish dialog/pricing panel/preview). Advanced vendor features ⏸️ with evidence: RecipeDetail context census is ONLY UI/INSTR/PK/BA/UB/IPT/FMT/GRP (105,408/29,147/19,103/11,916/11,908/59/3/1) — no vessel/phase/coating/Use-Group/Adjust/planning-task lines exist; RecipeDetailResource and Resource are 0 rows install-wide. Recipe-level IPT steps ⏸️ by observed usage: 59 lines ever, 58 on inactive recipes; order QC flows from ItemTest (13,524 rows) at order creation and create copies legacy recipe IPT lines as-is."

## L72 [FLIP_DONE] Verify / publish / activate, batch-record preview (recipes-batch)

**Evidence:** The note contains no pending item — it describes shipped, flow-tested behavior. Repo confirms every endpoint: recipes.controller.ts GET :id/preview (L61), :id/pricing (L72), :id/verify (L82), :id/publish-requirement (L90), POST :id/publish (L122), :id/active (L128); web /recipes/:id/preview reuses the shared BatchSheetView (apps/web/src/pages/RecipePreview.tsx). Publish auto-verifies, enforces single-active (rework exempt), e-sig via the operator-tunable recipe.publish secured item, order-creation enforcement closed, concurrency-hardened under the shared advisory lock, flow-tested end-to-end per the note itself. Nothing remains.

**Action:** Replacement note (trim, keep facts): "Publish (POST /recipes/:id/publish) runs Verify first (product line, ≥1 ingredient, complete lines, revision comment; standalone GET /recipes/:id/verify) then enforces single-active-recipe in the same tx (rework exempt both ways, vendor 7.21); gated by the operator-configurable recipe.publish secured item (reason default, signature/witness enableable → hash-chained ESignature). Activate/Deactivate (POST /recipes/:id/active) published-only; re-activating an old revision atomically deactivates the current. No unpublish (vendor-confirmed; revise = clone). Batch-record preview: GET /recipes/:id/preview?batchSize= renders the batch-sheet payload from RecipeDetail (scaled, grams-threshold, ItemTest QC specs); web preview reuses BatchSheetView with rescale + no-lot banner (§5.1.14). POST /orders rejects drafts + superseded revisions; picker filters them. Concurrency-hardened (advisory lock + in-tx re-read: no double publish / two-active / edit-vs-publish races). Flow-tested end-to-end."

## L74 [FLIP_DONE] Packaging recipes & Packaging Recipe Library (recipes-batch)

**Evidence:** Repo: RMPP handled across recipe-editor.service.ts, recipes.service.ts, recipe-replacement.service.ts (same lifecycle). New DB evidence closes the library half: SELECT Context, COUNT(*) FROM Recipe GROUP BY Context → RMBA 11,916 / RMPP 7,151 / RMPR 36 / RMBAL 1 — there is NO packaging-library (RMPPL) context in the entire install; zero packaging-library recipes were ever created (vs the batching library's single empty DEFAULT row, already ⏸️ at line 73). ItemPackagedProduct (7,136 rows) mirrored + imported and driving specify-packouts + packaging lookup.

**Action:** Replacement note: "Same editor handles RMPP: flat PK root + UI components (verified: live RMPP recipes contain ONLY PK+UI lines), per-1-lb quantities, same publish/single-active/clone lifecycle (no rework exemption — none are rework). Packaging Recipe Library ⏸️ with evidence: Recipe context census (RMBA 11,916 / RMPP 7,151 / RMPR 36 / RMBAL 1) shows NO packaging-library (RMPPL) recipe ever existed in this install. ItemPackagedProduct (bulk→packout binding, 7,136 rows) mirrored + imported (full + log-driven sync), drives §5 specify-packouts + §6 packaging-product lookup; bound recipe resolves to the ACTIVE revision at read time (see ASSUMPTIONS §Packouts)."

## L75 [SPLIT] Recipe pricing & expected costs (recipes-batch)

**Evidence:** Done part: tier rollup live (GET /recipes/:id/pricing, recipe-pricing.ts unit-tested, web panel) — confirmed in recipes.controller.ts L72 / recipes.service.ts. Build part is genuinely load-bearing: (1) UI lines in active published recipes referencing a recipe-made item = 23,680 of 45,366 (52%); (2) distinct such sub-recipe ingredients = 5,702, of which only 83 have any PriceDetail supplier price and 0 have StandardCost; (3) Item.StandardCost and Item.PurchasePrice are 0/NULL on ALL 21,176 items (only ReplacementCost is populated, 12,334 items), so the current standardCost fallback (recipes.service.ts L366-377) never fires with a real value — over half of a typical rollup's lines price at nothing today; (4) Item.CostingRecipe set on 12,823 items, and 12,809 of them point at the currently ACTIVE revision (14 stale) → resolve-to-active-at-read-time semantics, same convention as ItemPackagedProduct.

**Action:** BUILD (size M): mirror Item.CostingRecipe into the ERP1 schema + legacy import (+ log-driven sync — it is absent from legacy-import.service.ts's Item mapping today). Extend the existing recipe-pricing rollup (apps/api/src/manufacturing/recipes.service.ts + recipe-pricing.ts): when an ingredient has no supplier price, resolve its costing recipe to the ACTIVE published revision at read time (matching the 12,809/12,823 live pointer semantics) and recurse the §5.3.1 rollup on its per-1-lb formula for a per-unit cost; memoize per request, cycle guard + depth cap; consider Item.ReplacementCost (12,334 populated) as the terminal fallback instead of the empty StandardCost. Surface 'costed from sub-recipe' per line in the web pricing panel. Extends the shipped pricing module only — no new UI surface beyond the panel annotation."

## L81 [FLIP_DONE] Create orders from recipes; import orders (recipes-batch)

**Evidence:** The trailer 'Multi-batch / live execution pending' is stale: guided + express execution shipped (§85/§86, endpoints POST :id/execution/express, :id/lines/:lineId/record, :id/reverse confirmed in orders.controller.ts), and multi-batch is ⏸️ with evidence at line 84 (Ordr.Parent NULL on all 75K orders). Vendor 'import orders' never used: SELECT Program, COUNT(*) FROM Log WHERE Program LIKE '%Import%' GROUP BY Program → only QuickBooksImportVendors (454) and QuickBooksImportCustomers (102) in 15 years; zero Log actions matching '%ImportOrder%'/'%OrderImport%'. Legacy order data import into ERP1 itself is done (75K/505K rows).

**Action:** Replacement note: "Ordr/OrdDetail mirrored + imported (75K/505K rows); unified Orders browser (type/search/hold/open filters, full line detail + party/item/recipe decoration). Native batch-order creation live: POST /orders (orders.create) scales RecipeDetail into OrdDetail (UI + PK × batch size, structural/instruction lines copied), seeds ItemTest OnProduction specs onto an IPT line as OrdDetailTest, born Not-started into batch-sheet → release → complete → close; atomic hash-chained audit, native ids ≥1e9. Finished-good lot minted at creation per plant convention YYMMDD### under the id-alloc lock, linked via Lot.OrdDetail + stamped on Ordr.ManfLot. Vendor order-file import ⏸️ with evidence: the only import programs in 15 years of Log are QuickBooksImportVendors (454) / QuickBooksImportCustomers (102) — no order-import program ever ran. Multi-batch ⏸️ (§5 variance row: Ordr.Parent NULL on all 75K); execution live (§5 execution rows)."

## L82 [FLIP_DONE] Release, specify packouts, print batch sheets (recipes-batch)

**Evidence:** All three halves are live and the note carries no pending marker. Release: POST lifecycle endpoint (orders.release) live per line 83's verified lifecycle. Batch ticket: GET /orders/:id/batch-sheet + web page apps/web/src/pages/BatchSheet.tsx with a Print button calling window.print() (L88), validated field-for-field vs the plant's PDF (order 189170); execution-recorded IPT results now print on it (§86). Specify packouts: Packouts panel + POST /orders/:id/packouts creating the MFPP from the ACTIVE packout recipe and allocating bulk via native-id OrdDetailCommit in one tx — the exact linkage all 27,866 live commitments use; editable Supplied Qty for multi-batch supply.

**Action:** Replacement note: keep the existing text essentially as-is (it is accurate), appending: "Web batch-sheet page (BatchSheet.tsx) prints via the browser (Print button), and prints recorded IPT results (§86). All parts live — release (§ lifecycle), ticket validated field-for-field vs the plant PDF (order 189170), specify-packouts with over-packout warning + multi-batch Supplied Qty."

## L83 [FLIP_DONE] Complete/close with workflow approvals (recipes-batch)

**Evidence:** Complete/close live (orders.release/.complete/.close endpoints, invalid-transition guards, hash-chained audit, e-sig on Complete via order.complete secured item — validated on order 189299). Workflow-approval chains: plant never used them. Base tables Workflow, WorkflowDetail, Approval, ApprovalDetail, RoleApprovalDetail, OrdrApprovedAddition, RecipeApprovedAddition ALL 0 rows (queried individually); every workflow/approval view (MFWorkflow, ApprovalStatus, OpenWorkflowDetails, BatchSheetWorkflow…) sits over these empty tables. 15 years of Log: the ONLY matching program is 'Approval Update' — exactly 6 rows, all within 21 seconds on 2014-02-05, all by the vendor's own account mar-kov\lex (installation testing); nothing in the 12 years since.

**Action:** Replacement note: "Order lifecycle live: Release (NST→RLS) / Complete (RLS→CMP, actual batch size + reason) / Close (CMP→CLS), each its own program (orders.release/.complete/.close) with invalid-transition guards + atomic hash-chained audit (validated end-to-end on order 189299). E-signature on Complete enforced: password re-auth (+ optional witness) → hash-chained ESignature, gated by the order.complete secured item. Multi-step workflow-approval chains ⏸️ with evidence: Workflow/WorkflowDetail/Approval/ApprovalDetail/RoleApprovalDetail/OrdrApprovedAddition/RecipeApprovedAddition are all 0 rows; the only Log hit in 15 years is 'Approval Update' — 6 rows in a 21-second window on 2014-02-05 under the vendor's own account (mar-kov\lex, setup testing), never used since."

## L84 [FLIP_DONE] Material variance analysis; multi-batch (recipes-batch)

**Evidence:** Both halves already resolved; only the status is stale. Variance report live: GET /orders/:id/variance (planned QtyReqd vs actual QtyUsed, Δqty/Δ%/Δcost at real consumed unit cost from genealogy edges, yield line), gated by orders.variance, web panel on production orders — all present in orders.controller/service. Multi-batch already carries defer-grade evidence in the note itself: Ordr.Parent NULL on all 75K orders (re-verified during the packouts work; ASSUMPTIONS §'Express execution + multi-batch decision'); the plant sizes each batch to demand and splits packouts via Supplied Qty, which shipped specify-packouts supports; the column is mirrored.

**Action:** Replacement note: keep the existing text verbatim — it already states the variance build and the multi-batch deferral with evidence (Ordr.Parent NULL on all 75K orders, mirrored column, creation/roll-up UI deferred per the unused-module rule). Flip status 🟡→✅.

## L85 [FLIP_DONE] Full Batch Execution (preweigh, resources, procedure, release, express blend, packaging, reversing, yields, review, scale, auto next/scale) (recipes-batch)

**Evidence:** Trailer 'Preweigh/resources/express/scale pending' resolves entirely. Express shipped (§86, POST :id/execution/express confirmed). Preweigh never used: OrdDetail.MustPreweigh=1 on 0 of 505K lines; RecipeDetail.MustPreweigh=1 on 0 lines; Ordr.PreweighOnly=1 on 0 orders (Ordr.PreweighZone=1 on 56,086 orders is the stamped ParamsOrder default, not usage); MFDispensesTable 0 rows; every preweigh object (BatchSheetPreweigh, MFOrdDetailPreweigh, ContainerPreweighed, OutstandingPreweighs, PreweighProductivity) is a VIEW over empty bases; zero Log programs/actions containing 'Preweigh' in 15 years. Resources never used: Resource 0 rows install-wide, OrdDetailResource / OrdDetailTempResource / RecipeDetailResource / ResourceCalendar all 0 rows. Scale integration never used: WorkstationScale 0, CustomScaleDriver 0, Scale/ScaleConfig/ScaleInfo/Scales are views over empty bases, zero scale Log programs — dispenses were handwritten on the paper ticket (consistent with the IPT-results memory). 'Auto next/scale' is scale-hardware UI choreography → same defer.

**Action:** Replacement note: keep the existing guided-execution/reversal text, replacing the final 'Preweigh/resources/express/scale pending' with: "Express shipped (see next row). Preweigh / resources / scale integration (incl. auto next/scale) ⏸️ with evidence: MustPreweigh never set (0 of 505K OrdDetail, 0 RecipeDetail), Ordr.PreweighOnly=0 everywhere (PreweighZone=1 on 56,086 orders is the stamped ParamsOrder default), MFDispensesTable 0 rows and every preweigh view empty, no 'Preweigh' Log program/action in 15 years; Resource is 0 rows install-wide and OrdDetailResource/RecipeDetailResource/ResourceCalendar all 0; WorkstationScale and CustomScaleDriver 0 rows, no scale Log programs — the plant weighed on paper." Flip 🟡→✅.

## L86 [FLIP_DONE] Express Execution; batch testing (recipes-batch)

**Evidence:** Both halves live with no pending marker in the note. Endpoints confirmed in orders.controller.ts: POST :id/execution/express (L309), POST :id/ipt-results (L317). Batch sheet prints recorded results; execution panel has the result grid + Express confirm control per the note. Nothing in the row's meaning is outstanding — legacy comparison points (paper-recorded results, FIFO trade-off) already documented in the note and ASSUMPTIONS §Express execution.

**Action:** Replacement note: keep the existing text verbatim (it is current and complete); flip status 🟡→✅.

## L92 [FLIP_DONE] Packaging: create/release/print/complete (recipes-batch)

**Evidence:** The trailer 'Package Execution setup/end-lot specifics, express, order edits pending' is triple-stale: express on MFPP ✅ (line 93: same engine, one tx, FIFO + lineage); packaging order edits ✅ (line 94: same revisions engine, EDT lock, e-signed publish); end-lot/setup + reserved-material release ⏸️ with evidence (line 93, dated 2026-07-03: Phase NULL on every MFPP OrdDetail, LocationVessel 0 rows, ReserveAmount only on 45 unrelated SH orders). Create/release/print/complete themselves are live: shared POST /orders create engine (Context=MFPP derived from recipe context), shared batch-sheet/release/complete(e-sig)/close path, guided execution works on MFPP, packaging-product lookup (GET /orders/packout-options) live.

**Action:** Replacement note: keep the existing create/execution/lookup text, replacing the final sentence with: "Package Execution end-lot/setup specifics + reserved-material release ⏸️ with evidence (next row); express ✅ (next row); packaging order edits ✅ (§ Packaging order edits row)." Flip 🟡→✅.

## L99 [DEFER] Controlled substances: lot/container reconciliation, tare correction, reverse disposal (recipes-batch)

**Evidence:** Queries (all against CMS, read-only): SELECT ControlledSubstance, COUNT(*) FROM Item GROUP BY ControlledSubstance → False 7,411 / NULL 13,765 — TRUE on 0 of 21,176 items, so no item was ever flagged a controlled substance. Lot reconciliation fields untouched on all 55,854 lots: DateReconciled NOT NULL = 0, ReconciliationStatus NOT NULL = 0, ReconciliationFailed=1 = 0. ReconciliationStatus table = 0 rows; LotReconciliationReport/Detail are views over empty bases. Tare correction never used: InvMovement.TareEntered=1 on 0 rows; Location.TareCorrectionRequired=1 on 0 locations. 15 years of Log contain no program or action matching '%Controlled%' or '%Reconcil%' (the only 'Reconcil' Log/table hits are QuickBooks till/transaction reconciliation views, also empty).

**Action:** Replacement note: "⏸️ Never used at this plant (2026-07-09 evidence): Item.ControlledSubstance is True on 0 of 21,176 items (7,411 False / 13,765 NULL); the Lot reconciliation fields are untouched on all 55,854 lots (DateReconciled / ReconciliationStatus all NULL, ReconciliationFailed on none) and the ReconciliationStatus table is 0 rows; tare correction never used (InvMovement.TareEntered on 0 rows, Location.TareCorrectionRequired on 0 locations); 15 years of Log contain no program/action matching '%Controlled%' or '%Reconcil%'. UG ch.10 wholly unused — deferred per the unused-module rule; don't rebuild without a user ask."

## L104 [DEFER] Rooms, vessels, pails, equipment, scales, abstract resources (resources-documents)

**Evidence:** Resource = 0 rows, and every satellite table is also empty: ResourceCalendar, ResourceCategory, ResourceControl, ResourceScheduleGroup, ResourceScheduleGroupDetail, ResourceValve, RecipeDetailResource, OrdDetailResource, OrdDetailResourceEdit, OrdDetailTempResource all 0 rows (sys.partitions sweep) — no recipe or order ever referenced a resource. Log (15 years): SELECT Program, COUNT(*) FROM Log WHERE Program LIKE '%Resource%' OR '%Room%' OR '%Vessel%' OR '%Scale%' OR '%Equip%' returns zero rows.

**Action:** Replacement note: "**Never used at this plant**: `Resource` 0 rows and all ten satellite tables (ResourceCalendar/Category/Control/ScheduleGroup(+Detail)/Valve, RecipeDetailResource, OrdDetailResource/Edit/Temp) 0 rows — no recipe or order ever referenced a resource; zero Log rows in 15 years for any Resource/Room/Vessel/Scale/Equip program. Deferred per the unused-module rule."

## L105 [DEFER] Resource labels (resources-documents)

**Evidence:** There are no resources to label: Resource = 0 rows install-wide; zero Log rows in 15 years for any resource-related program (same Log query as row 104).

**Action:** Replacement note: "**No data behind it**: labels print for `Resource` records and this install has zero (`Resource` 0 rows, zero Log usage of any resource program in 15 years). Deferred with the resource module."

## L106 [DEFER] Scheduled & unscheduled maintenance, recording, availability, notifications, history (resources-documents)

**Evidence:** MaintDetail (the actual maintenance-event records) = 0 rows; MaintScheduleGroupItem = 0; MaintScheduleItemSpec = 0. Only vestigial config survives: MaintScheduleGroup 3 rows ('Vessel Cleaning', 'Nut Cleaner Maintenance Schedule', 'Monthly Mixer Maintenance') + MaintScheduleItem 2 rows ('Breakdown', 'Sanitize'), bound to no Resource (Resource = 0 rows). ParamsCMS.VesselAndEquipMaintRequired = False. Log (15 years): SELECT Program, COUNT(*) FROM Log WHERE Program LIKE '%Maint%' returns zero rows.

**Action:** Replacement note: "**Never used**: `MaintDetail` (actual maintenance records) 0 rows, `MaintScheduleGroupItem`/`MaintScheduleItemSpec` 0 rows — only 5 vestigial config rows (3 MaintScheduleGroup + 2 MaintScheduleItem) bound to no resource (`Resource` 0 rows); `ParamsCMS.VesselAndEquipMaintRequired` = False; zero Log rows in 15 years for any Maint program. Deferred per the unused-module rule."

## L107 [DEFER] Scanned maintenance sheets (resources-documents)

**Evidence:** Document.TableName distribution is exactly {Ordr 27,844; ChangeSet 18,243; Waybill 17,517; Comment 47} — zero documents reference any Maint table; MaintDetail = 0 rows (no maintenance ever recorded to scan a sheet for); the scan-import config table WorkstationScandocParams = 0 rows; zero Log rows for any Maint or Scan program in 15 years.

**Action:** Replacement note: "**Never used**: no `Document` row references any Maint table (TableName distribution is Ordr/ChangeSet/Waybill/Comment only), `MaintDetail` 0 rows, and the scan-import config (`WorkstationScandocParams`) is empty. Deferred with the maintenance module."

## L112 [FLIP_DONE] Shipping Orders (quotes, POS) (sales-shipping)

**Evidence:** Everything except quotes/POS is already built (native SH create, line edits, ship-lot capture, price sourcing). Quotes: SELECT IsQuote, COUNT(*) FROM Ordr WHERE Context='SH' -> IsQuote=0 on all 15,739 SH orders ever; Ordr contexts are only MFPP/MFBA/SH/PO (no quote context); zero Log programs LIKE '%Quote%'. POS: SELECT COUNT(*) FROM TransPayment -> 0 rows; PaymentMethod has only 5 seed rows; 15 years of Log (667,393 rows) contain no program matching '%POS%', '%Till%', '%Payment%', '%Cash%' or 'Complete Sale'. The plant never quoted or ran counter sales.

**Action:** Replace the trailing 'Quotes / POS pending' with: 'Quotes & POS deferred by evidence: `Ordr.IsQuote` = 0 on all 15,739 SH orders ever (no quote context exists either); POS/counter sales never happened — `TransPayment` 0 rows, `PaymentMethod` only 5 seed rows, and 15 years of Log (667K rows) have no quote/POS/till/payment/complete-sale program.' Row flips to ✅.

## L113 [BUILD] Reserve/unreserve containers, shipping assemblies (sales-shipping)

**Evidence:** Heavily and currently used. Log: SELECT Program, COUNT(*) FROM Log ... -> 'Shipping Assembly' 15,855 (2026 YTD alone: 1,916; every year 2020-2026), 'PrintShippingLabel' 17,666. Location Context='ASM' = 17,664 rows. Reservation = Inventory.OrdDetail: 12 parcels reserved RIGHT NOW, all on open SH order 189526 (status RTS, one of 38 open RTS SH orders), physically staged in ASM locations (Inventory joined to Location: 12 rows in ASM context). Note: containers-as-CNT do not exist in this install (Location Context='CNT' = 0 rows; contexts are VSL/SMP/ASM/WHS/ZON/LCN/PAIL) — reservation operates at inventory-parcel/lot level, so no container model is needed. ERP1 already mirrors both backing columns: Inventory.ordDetailId and Location.context in packages/db/prisma/schema.prisma; no reservation/assembly code exists in apps/api/src (all 'reserv' hits are unrelated).

**Action:** Build the pre-shipment staging workflow on the existing Orders/SH module (size M): (1) reserve/unreserve on-hand lot parcels to an open SH line — set/clear the already-mirrored `Inventory.ordDetailId` under the valuation-engine locking conventions, reusing the existing ship-lot-options picker for candidates; (2) native shipping assemblies — create/name a `Location` Context='ASM' row (native id >= 1e9) and move reserved parcels onto it (reuses the existing transfer-parcel move + movement emission); (3) at order close, pre-fill ship-lot capture from the line's reservations (reserved-first picking) and clear reservations on shipment; (4) a 'Reserve / stage' panel on the open SH order detail + assembly label print. Scope note for the row: parcel-level reservation only — CNT containers have 0 rows in this install.

## L114 [FLIP_DONE] Waybills, invoices (sales-shipping)

**Evidence:** The only pending item in the note is '3rd-party shipping integration pending', and that was never used: SELECT ShipIntegration, COUNT(*) FROM Entity -> NULL on all 929 entities (ShipIntegrationReference likewise empty); SELECT ... FROM WaybillTrackingNumber -> all 17,910 rows are 'None'/empty, zero real tracking numbers in 15 years; Log Program='Waybill' actions -> 70,454 manual WaybillDataSet.SaveChanges vs 3 ProcessOutstandingRequests ever; FreightService on Waybill is hand-picked (16,530 of 17,495 = 'None'). Everything else in the row (CI invoice browser+doc, packing slips via SH ChangeSet->ChangeSetShipment->Waybill, AP bills, native invoice generation with reversal-pair-aware billed-qty math) is verified shipped in apps/api/src/sales/invoices.service.ts / packing-slip.service.ts.

**Action:** Replace the trailing '3rd-party shipping integration pending' with: '3rd-party shipping integration ⏸️ (see dedicated row below): never used at this plant — `Entity.ShipIntegration` NULL on all 929 entities, all 17,910 `WaybillTrackingNumber` rows are None (no carrier system ever wrote back), and the Waybill program logged 70,454 manual SaveChanges vs 3 ProcessOutstandingRequests in 15 years.' Row flips to ✅.

## L115 [SPLIT] Warehouse/lab transfers, bill-and-hold, returns/credits (sales-shipping)

**Evidence:** WAREHOUSE TRANSFERS — USED: Trans Context='TI' = 182 transfer invoices (T-sequence docs), active every year 2020-2026 (16 in 2026, latest 2026-07-08): 107 to PRESS TECH CONSIGN (IsWarehouse=1, InTransitLocation=31285), 71 to SCHIELE CONSIGNMENT (through 2024), 4 own-site; mechanism verified: plain SH order with ShipTo=warehouse entity and BillTo=CMSSITE (own site) invoiced as TI; consigned WHS locations owned by the warehouse entity hold live stock (PRESS TECH: 4 non-zero parcels); InvMovement Context='TRNSFR' = 581. ERP1 already renders TI in invoice detail (INVOICE_CONTEXTS=['CI','TI'] in apps/api/src/sales/invoices.service.ts) but the list filters context='CI' and generation is CI-only. RETURNS/CREDITS — USED: 96 negative-QtyReqd SH lines (customer returns, recurring, latest 2026-04-01); credits = invoice reversal pairs: 2,343 CI rows with ReversedTrans set (330 in 2026 YTD, growing), same TransDocument re-billed same day; InvMovement Context='RVSSH' (reverse shipment) = 1,406. ERP1 excludes reversal pairs in billed-qty math but cannot create a reversal, and SH line DTOs enforce @IsPositive() qty (create-shipping-order.dto.ts / edit-sh-line.dto.ts) so return lines are impossible today. LAB TRANSFERS — NEVER: SELECT SUM(IsLab) FROM Entity -> 0 lab entities install-wide. BILL-AND-HOLD — NEVER: no column LIKE '%BillAndHold%'/'%BillHold%' anywhere in INFORMATION_SCHEMA.COLUMNS; zero Log programs LIKE '%Hold%'; Ordr.NoBill never set on any of 15,739 SH orders.

**Action:** SPLIT. (a) BUILD warehouse transfers (size S-M): extend the existing invoice generator to mint Trans Context='TI' (T-sequence number, BillTo=own site) when the SH order's ShipTo is an IsWarehouse entity; at ship-lot close for warehouse ship-tos, RELOCATE the shipped parcels into the destination entity's consigned WHS location (already mirrored) instead of depleting; list TI in the invoice browser (detail already renders it). (b) BUILD returns/credits (size M): allow negative-qty return lines on SH orders end-to-end (relax the @IsPositive validators, ship-lot capture accepts returned lots INTO stock with RVSSH-style movement legs, invoice generation bills the negative = credit) + a native 'reverse invoice' action mirroring the legacy pattern (new Trans pointing at the original via ReversedTrans, same document number, restores invoiceable qty — the billed-qty math in invoices.service.ts already understands pairs; machinery parallels the existing order/receipt reversal endpoints). (c) DEFER lab transfers: '0 IsLab entities exist install-wide — lab transfers structurally impossible/never used.' (d) DEFER bill-and-hold: 'no bill-and-hold column exists anywhere in the legacy schema, no Hold-related program in 15y of Log, and Ordr.NoBill is null on all 15,739 SH orders.'

## L116 [DEFER] Till reconciliation (sales-shipping)

**Evidence:** Till reconciliation reads cash-sale payments and there have never been any. TillReconciliationReport is a VIEW joining Trans to TransTotalPayments/RefCompleteSaleLog (view definition inspected); its base table TransPayment has 0 rows (SELECT COUNT(*) FROM TransPayment -> 0); PaymentMethod holds only 5 seed rows; 15 years of Log contain no program matching '%Till%', '%Payment%', '%Cash%', '%POS%' or 'Complete Sale' (the only '%Sale%' hits are Sales Rep Update 14 / Salesman Update 8 / SalesAnalysis 4). The report would render empty for the plant's entire history.

**Action:** Note text: '⏸️ Never used: till reconciliation reconciles POS cash-sale payments, and this install recorded zero — `TransPayment` 0 rows ever (the `TillReconciliationReport` view reads it via TransTotalPayments), `PaymentMethod` only 5 seed rows, and 15 years of Log (667K rows) have no till/payment/cash/complete-sale program. Revisit only if POS is ever adopted.'

## L117 [DEFER] 3rd-party shipping software integration (sales-shipping)

**Evidence:** Never configured, never wrote back, never polled. SELECT ShipIntegration, COUNT(*) FROM Entity GROUP BY ShipIntegration -> NULL on all 929 entities (ShipIntegrationReference also empty); SELECT CASE WHEN TrackingNumber IN ('None','') ... FROM WaybillTrackingNumber -> 17,910 of 17,910 rows empty/'None' — no carrier software ever returned a tracking number in 15 years; Log Program='Waybill' by Action -> 70,454 manual WaybillDataSet.SaveChanges vs 3 WaybillDataSet.ProcessOutstandingRequests (the integration polling hook) ever; Waybill.FreightService is hand-entered (16,530 of 17,495 waybills = 'None', rest typed carrier service names).

**Action:** Note text: '⏸️ Never used: `Entity.ShipIntegration`/`ShipIntegrationReference` NULL on all 929 entities; all 17,910 `WaybillTrackingNumber` rows are None (zero carrier write-backs in 15 years); the integration hook ran 3 times ever vs 70,454 manual waybill saves (Log Program=Waybill by Action). Shipping is done manually with carrier service typed on the waybill; nothing to integrate.'

## L129 [BUILD] Tests & test groups; testing requirements by item (lims-viewers)

**Evidence:** Repo: ItemTest viewer+editor live (ItemTestsService, programs qa.itemTests/qa.itemTestsEdit, Item Tests page), Test/TestGroup mirrored+imported (35+4 rows), catalog-backed test-name picker — all verified in apps/api/src/qa/item-tests.service.ts + apps/web/src/pages/ItemTests.tsx. Legacy: the note's 'catalog admin deferred until needed' is stale — 'Test Update' IS the Test-catalog editor (request XML shows Object="Test" Insert/Delete) with 76 uses from 2014 to 2025-12-23, still adding tests (Test 'IR UPCONVERT' inserted 2025-12-23, 'BROOKFIELD #4 @20RPM' 2025-09-24). No TestGroup-editor program ever logged in all retained Log (2014->2026); TestGroup is 4 stable rows (plain LOT/BATCH sampling).

**Action:** Build the Test-catalog admin (the only remaining gap; everything else in the row is done): CRUD for the Test master on ItemTestsService — name, description, result type NUM/BOOL, precision, test group, prototype flag — new program qa.testCatalogEdit, audited, native ids (>=1e9, re-import-safe), plus a small Catalog section on the existing Item Tests page; the picker already merges catalog+ad-hoc so new rows appear automatically. TestGroup stays import-only (never edited in 12.5 yrs of Log). Size S.

## L130 [SPLIT] Sampling (sample sets, labels, sampling, IPT, retesting) (lims-viewers)

**Evidence:** IPT flips DONE: OrdDetailTest order QC specs are created from ItemTest OnProduction at native order creation and native IPT result entry exists (orders.service.ts recordIptResults; ASSUMPTIONS batch-exec #4 — results are an ERP1 extension, legacy had no result column). Retesting DEFERS: ItemTest.OnRetest=1 on 0 of 13,524 rows; Item.RetestPeriod nonzero on 0 of 21,176 items; Release.Status ever recorded = Approved 54,751 / Hold 26,449 / Rejected 68 only (no retest state); TestGroup.RetestSamplingMethod=None on all 4 groups. Sample-set creation BUILDS — it is the plant's daily loop and ERP1 cannot do it post-cutover: SampleSet 25,416 rows still growing (Release #84348/SampleSet #27076 dispositioned 2026-07-08); sets are auto-created SERVER-SIDE inside receipt/batch transactions (zero Log requests in 12.5 years ever insert SampleSet; no program matching %Sampl% exists in Log); Release 81,268 rows (25,414 linked to sample sets) and ERP1 has no prisma release.create anywhere. Labels: Container table = retained QC sample parcels exactly 1:1 with SampleSet (25,416 rows; LocationSample 25,416); PrintContainerLabel = 25,434 uses, last 2026-07-08 — the sample-label print is live daily. Seams the plant uses: 'Purchase Receipt' 9,134 uses (active), 'Miscellaneous Receipt' 820, batch close (Disposition Sublot 25,458 / Enter Test Results 25,517, both through 2026-07-08).

**Action:** Build native sample-set creation (M): at the three seams ERP1 already owns (purchasing.receive, misc-receipt, order completion/batch close), for each new sublot create the Release row (Hold when the item has ItemTests, matching legacy's tested-lot flow), mint a native sampleSetId and pre-create LocationSampleTest rows from the item's ItemTest specs — exactly the shape ReleasesService.enterResults already expects — emit the install's configured 'New Sample set' notification (rule #10, currently the only configured QA code left unwired), and add a printable sample label (PrintContainerLabel parity). Extends qa/releases + the existing receipt/completion engines; atomic + audited + native ids. Retesting portion: defer with the OnRetest=0 / RetestPeriod=0 evidence.

## L131 [DEFER] Sample receiving; stability testing; custom sampling (lims-viewers)

**Evidence:** All three never used in this install, verified live: SampleReceipt view 0 rows; StabilityTestReceipt 0 rows; SampleSet.IsStability=false on all 25,416 sample sets; TestSchedule 0 rows; LimsTestGroup 0 rows; TestGrouping 0 rows; zero Log programs matching %Sampl% or %Stabil% in all retained Log (2014->2026); all 4 TestGroups use the plain LOT/BATCH sampling methods (SamplingMethod=LOT, MFSamplingMethod=BATCH/LOT, RetestSamplingMethod=None, SampleSize 0.005 kg) — no custom sampling ever configured. ERP1's notification catalog already records 'Sample receipt'/'Reverse sample receipt'/stability codes as unwired-with-reason.

**Action:** Note text: 'Never used in this install (verified live): SampleReceipt 0 rows, StabilityTestReceipt 0 rows, SampleSet.IsStability=false on all 25,416 sets, TestSchedule/LimsTestGroup/TestGrouping all 0 rows, no %Sampl%/%Stabil% program in 12.5 yrs of retained Log; all 4 TestGroups use plain LOT/BATCH methods (no custom sampling). Stability/sample-receipt notification codes catalogued as unwired with this evidence.'

## L132 [SPLIT] Enter test results; disposition sublot; at-risk (lims-viewers)

**Evidence:** Results entry + disposition FLIP DONE (verified in apps/api/src/qa/releases.service.ts): enterResults updates LocationSampleTest vs ItemTest spec with pass/fail + tester stamp + audit; disposition is e-signed (release.disposition secured item), rides the shared ApprovalRequest engine (request->approve/reject, separation of duties), and emits 'Release Sublot Notification' + 'Tests Completed Notification' at the vendor's UG 22.2.3 seams. At-risk DEFERS: Container.AtRisk=1 on 0 of 25,416 containers; Release.Status ever = Approved/Hold/Rejected only (81,268 rows, no at-risk state); no at-risk program in all retained Log. Creating new sample sets is the row-130 build (ERP1 never creates Release rows natively today — no prisma release.create in the repo).

**Action:** Update note: results + disposition + approval enforcement done; at-risk deferred with 'Container.AtRisk=1 on 0 of 25,416 containers, Release statuses ever recorded = Approved 54,751/Hold 26,449/Rejected 68, no at-risk program in 12.5 yrs of Log'; sample-set creation -> see the §130 sampling build (this row flips ✅ when that lands).

## L133 [SPLIT] Print Certificate of Analysis; auto expiry; reduced testing; QA notifications (lims-viewers)

**Evidence:** CofA FLIP DONE (already live per note; Certificates + CofADoc pages verified in repo). QA notifications FLIP DONE: ERP1 natively emits 'Release Sublot Notification', 'Tests Completed Notification' (releases.service.ts) and 'Testing Required Notification' (the install's configured rule #8); of the install's 14 configured Notification rules the ONLY QA code left unwired is 'New Sample set' (rule #10) which is wired as part of the row-130 sampling build; the remaining unwired QA codes (QA Cancel, Reduce Testing Toggled, 3 stability codes) belong to never-used features. Auto expiry DEFERS — expiry data does not exist anywhere in this install: Release.ExpiryDate NULL on all 81,268 rows, Container.ExpiryDate NULL on all 25,416 rows, ItemManufacturer.ExpiryDate 0 rows, Item.RetestPeriod nonzero on 0 of 21,176 items, and 13,775 of 21,176 items are explicitly flagged NoExpiry. Reduced testing DEFERS: Lot.ReduceTesting=1 on 0 rows; 'Reduce Testing Toggled' not among the 14 configured notification rules; TestGroup.RetestSamplingMethod=None on all 4 groups.

**Action:** Update note: CofA + QA notifications done (Release Sublot / Tests Completed / Testing Required wired; 'New Sample set' rides the §130 sampling build); auto expiry ⏸️ 'zero expiry data install-wide: Release.ExpiryDate NULL on all 81,268, Container.ExpiryDate NULL on all 25,416, ItemManufacturer.ExpiryDate 0, RetestPeriod 0 on all items, 13,775/21,176 items flagged NoExpiry'; reduced testing ⏸️ 'Lot.ReduceTesting=1 on 0 rows ever; code not configured'. Row flips ✅/⏸️ clean once the §130 build lands.

## L138 [DEFER] Scan/import batch records, CofA, MSDS/SDS, maintenance, waybills (resources-documents)

**Evidence:** The scan-import pipeline was never configured: WorkstationScandocParams (per-workstation source/accepted/rejected dirs, PDF-to-TIF, barcode params) = 0 rows; Log (15 years) has zero rows for Program LIKE '%Scan%'/'%Document%'/'%Attach%'. Document = 63,649 rows but SUM(Data IS NOT NULL) = 0 install-wide — 63,602 are auto-minted print-REGISTRY pointers for documents the system itself generated (27,844 'Batching Order' on Ordr; 18,243 ChangeSet Role='Shipment' packing slips; 17,517 Waybill; all FileType NULL, Data NULL). The only files ever imported are the 47 comment attachments (row 139) — zero batch records, CofA, or MSDS/SDS were ever scanned. Documentation (17,991 rows) is the built-in UDT data dictionary, and EntityDocument (4,284) is per-customer sales-doc distribution flags — neither is scanned-document data.

**Action:** Replacement note: "**Scan-import never configured or used**: `WorkstationScandocParams` (the per-workstation scan pipeline config) 0 rows; zero Log rows in 15 years for any Scan/Document program; `Document` (63,649) carries NO file content anywhere (`Data` NULL install-wide) — 63,602 rows are print-registry pointers for system-GENERATED docs (27,844 batching orders, 18,243 packing-slip ChangeSets, 17,517 waybills), which ERP1's print-faithful documents already cover; zero batch records/CofA/MSDS ever scanned (`Documentation` = the UDT data dictionary; `EntityDocument` = customer doc-distribution flags, not scans). Deferred per the unused-module rule."

## L139 [DEFER] Attach documents to comments; document viewers (resources-documents)

**Evidence:** Comment = 48 rows total in 15 years (TableName: Ordr 20, Waybill 19, Entity 3, Item 3, Recipe 2, ChangeSet 1); Log Program='Comment' = 54 executions, 2018-12-27 to 2022-07-11, none in the 4 years since. Attachments: 47 Document rows Role='Attachment' (46 pdf + 1 png) — one 2018 logo plus 46 Xerox scans of shipping paperwork (BOLs/pick lists) in a single Feb-Jul 2022 burst. The file bytes are NOT stored in the database: Document.Data is NULL for all 63,649 rows; the scans survive only as EncodedBase64 inside the Log request XML (verified on Log 441502, a 'SaveDocument' step carrying the full PDF base64).

**Action:** Replacement note: "**One abandoned burst, no stored bytes**: `Comment` 48 rows / 54 Log executions in 15 years (Dec 2018-Jul 2022, dead 4 years); 47 attachments total (46 PDF + 1 PNG) — 46 are Feb-Jul 2022 Xerox scans of BOLs/pick lists clipped to Waybill/Ordr comments; `Document.Data` is NULL install-wide, so the only surviving copies are base64 blobs inside Log request XML (e.g. Log 441502). Deferred per the unused-module rule; if ever revived, ERP1 would build a native attachment store (nothing migratable from legacy) and the bytes are recoverable from Log XML."

## L151 [FLIP_DONE] Entity/installation/site config (masterdata-config)

**Evidence:** The hierarchy is a fixed 3-row chain: CMS(1) → CMSINSTALL(2) → CMSSITE(3) — exactly one IsCMS, one IsInstallation, one IsSite entity. The legacy 'CMS' config-entity program was used 84 times, LAST on 2016-08-31 — untouched for a decade ('Entity Update' 2 ops, last 2014). ERP1 imports the hierarchy and consumes it live: notification engine walks owners up the entity hierarchy (notification-engine.service.ts), and purchasing/planning/sales resolve the owner/site entity (resolveOwnerEntityId in purchasing.service.ts, planning-recalc.service.ts, shipping.service.ts); entity editing lives in the Entities admin.

**Action:** Replacement note: 'Entity IsCMS/IsInstallation/IsSite — the fixed 3-row config chain (CMS→CMSINSTALL→CMSSITE, one of each) imports and is consumed live (notification owner-walk, owner/site resolution in purchasing/planning/shipping); entity editing in the Entities admin. Create-config-entity flow ⏸️ by evidence: legacy CMS program last ran 2016-08-31 (84 uses total) — single-site install, hierarchy static for a decade.'

## L153 [SPLIT] Document logo/branding; bins, location groups, storage rules, units, zones (masterdata-config)

**Evidence:** Logo IS used, contra the defer-leaning note: every legacy customer-facing document embeds the company logo image — InvoiceReport 17,928/17,928 printed invoices carry an identical 28,250-byte Logo blob; PackingSlipReport (17,835 rows) and OrderConfirmationReport (15,739) have the same Logo column; Report.UseLogo=1 on 25 of 108 report definitions. ERP1 documents currently print only the configurable text company header. The deferred sub-parts re-verified live: LocationGroup 0 rows, Workstation 0 rows, StorageRule 9 rows with ParamsPurchaseReceipt.ValidateStorageRules=False (never enforced), Bin Update 7 ops ever (last 2020-09-25), zones = 3 static ZON Location rows; Units ✅ (master units editor).

**Action:** BUILD (S): company-logo upload — an image app-setting (data-URI, size-capped) on the existing Configuration page company/document-header group, rendered in the shared document header of the print-faithful templates (invoice, packing slip, PO, CofA), matching the legacy docs where all 17,928 invoices print the logo. Keep the rest deferred as noted (units ✅; bins/location groups/zones/storage rules ⏸️ with the re-verified evidence above).

## L165 [FLIP_DONE] §16 Users, roles, secured items, response levels (platform-security)

**Evidence:** Twin of §0 line 20; both pending sub-parts close by evidence. Row-segregation: 6 legacy SecurityGroup codes defined, zero rows ever assigned — Item 21,176/21,176 'None', Ordr 75,948/75,948 'None', Recipe 19,104/19,104 'None', StorageRule 9/9 'None', Notification 14/14 '*' wildcard. Secured-item audit: complete — 80 legacy SecuredItems, 12 ever fired across 285,985 LogSecuredItem responses (Dec 2018→Jul 2026), 3 still enabled, signature volume only on OrderEditQtyExceeded (12,909) + YieldOutsideTolerance (4,602), both covered by ERP1's stricter always-on gates; SecuredItemCondition = 0 rows.

**Action:** Flip to ✅. Replacement note: "Users admin (create + status + group membership) + Roles admin (create + program grants) + Secured-item admin (response levels + perform/witness grants) all live; see §0. **Row-segregation ⏸️ by evidence**: legacy defines 6 `SecurityGroup` codes but zero data rows ever carried one (Item 21,176, Ordr 75,948, Recipe 19,104, StorageRule 9 — all 'None'; Notification 14 — all '*') — never used in this install. **Secured-item coverage audit done**: of 80 legacy secured items only 12 ever fired (285,985 responses, Dec 2018→Jul 2026); the two with signature volume (OrderEditQtyExceeded 12,909, YieldOutsideTolerance 4,602) map onto ERP1's stricter always-on `order.revise`/`order.complete` gates."

## L166 [FLIP_DONE] §16 Electronic signatures / witness, approvals, workflow (platform-security)

**Evidence:** Twin of §0 lines 21+24. Built and verified: e-sig capture on all five gates (order complete/reverse/revise, QA disposition, recipe publish); per-group approval policy + reusable blocking-approval engine uniformly adopted across four consumers with web queues. Deferral evidence for the pending sub-part (multi-step chains): all 10 legacy chain tables 0 rows (Workflow/WorkflowDetail/Approval/ApprovalDetail/RoleApprovalDetail/SecuredItemCondition/SignatureMeaning/OrdrApprovedAddition/OrdrApprovedAdditionEdit/RecipeApprovedAddition); only 6 'Approval Update' Log entries in 12+ years, all 2014-02-05 by vendor account mar-kov\lex. Witness: supported in ERP1 but never used in legacy (0 witness co-signs in 285,985 LogSecuredItem responses).

**Action:** Flip to ✅. Replacement note: "E-signature capture (re-auth + optional witness → hash-chained ledger) live on **all five** secured-item gates (order complete / reverse / revision publish, QA disposition, recipe publish), driven by operator-tunable response levels; per-user-group approval policy + the reusable blocking-approval engine uniformly adopted (see §0). Witness supported though the plant never used it (0 witness co-signs in 285,985 legacy secured-item responses). **Multi-step approval/workflow chains ⏸️ by evidence**: all 10 legacy chain tables are 0 rows and the only 'Approval Update' log entries ever are 6 rows from a single 2014 vendor setup session — never used. TOTP signing factor arrives with the Auth MFA build (§0)."

## L193 [FLIP_DONE] Read-only viewers (UG §23.1) (lims-viewers)

**Evidence:** Repo: every claimed covering page/viewer exists — Inventory.tsx, inventory-movement + inventory-at-date + shipment-detail in viewer-registry.ts, LotTracking.tsx (Lot Trace), Audit.tsx, QA disposition/results pages, PackingSlips.tsx. Legacy usage over ALL retained Log (2014->2026): Container Info, Trace Parents, Trace Children, View Release, Sublot Status, Inventory At Date, Log History = ZERO logged opens ever (viewers demonstrably log opens — set-viewer rows and assembly windows appear in Log); Generic Log Viewer 1 use (2019-04-02); Inventory History 1 use (2021-04-28); Assembly-info windows ('* - Assembly' 2,864 uses + ~30 'A0xxxxx - Assembly' windows) dead since 2020-04-30 with no Assembly base table in the DB and LocationVessel 0 rows; the Container table is exactly the 25,416 retained QC sample parcels (1:1 with SampleSet), surfaced via the QA/inventory pages.

**Action:** Replacement note: 'Covered by ERP1 pages where the plant ever used them: Inventory Browser/History → Inventory browser + inventory-movement (Inventory History: 1 use in 2021); Inventory At Date → inventory-at-date (0 legacy uses); Trace Parents/Children → Lot Trace (0 uses as standalone viewers; genealogy is lot-level in this install); Generic Log Viewer/Log History → Audit (1 use/0 uses); View Release/Sublot Status → QA disposition + results pages (0 uses); Shipment Viewer → Packing Slips + shipment-detail. Container/Assembly Info + container-level traces: assembly windows dead since 2020-04-30 (2,864 + ~30 uses, none in 6 yrs; no Assembly table, LocationVessel 0 rows) and Container rows are exactly the 25,416 retained QC sample parcels (1:1 with SampleSet) shown on QA pages — nothing left to build.'
